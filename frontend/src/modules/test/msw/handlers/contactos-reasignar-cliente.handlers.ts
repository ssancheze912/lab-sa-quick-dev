/**
 * Re-export from canonical handler location.
 * Needed because useReasignarContacto.test.ts uses a relative path that resolves here.
 */
export * from '../../../../test/msw/handlers/contactos-reasignar-cliente.handlers'
