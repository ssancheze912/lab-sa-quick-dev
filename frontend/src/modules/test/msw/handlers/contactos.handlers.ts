/**
 * Re-export from canonical handler location.
 * Needed because useDeleteContacto.test.ts uses a relative path that resolves here.
 */
export * from '../../../../test/msw/handlers/contactos.handlers'
