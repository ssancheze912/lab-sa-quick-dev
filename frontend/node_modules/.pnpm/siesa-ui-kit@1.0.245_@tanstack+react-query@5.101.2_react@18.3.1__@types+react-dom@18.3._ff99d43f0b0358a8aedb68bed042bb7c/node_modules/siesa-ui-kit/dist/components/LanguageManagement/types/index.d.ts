import type { ReactNode } from 'react';
/**
 * Representa un idioma disponible en el sistema.
 * Esta interfaz define la estructura que el backend debe retornar para listar idiomas.
 * Soporta múltiples nomenclaturas (español/inglés) para mayor flexibilidad.
 */
export type Language = {
    /** Código único del idioma (ISO 639-1). Ej: 'es', 'en', 'pt' */
    codigo?: string;
    /** Nombre legible para el usuario en español. Ej: 'Español' */
    nombre?: string;
    /** Código único del idioma (ISO 639-1) - Variante en inglés */
    code?: string;
    /** Nombre legible para el usuario - Variante en inglés */
    name?: string;
    /** Nombre nativo del idioma */
    nativeName?: string;
    /** URL opcional de un icono o bandera para mostrar en el selector */
    icon?: string;
};
/**
 * Diccionario de traducciones (Llave: Valor).
 * Soporta estructuras planas. Ejemplo: { "saludo.bienvenida": "¡Hola!" }
 */
export type TranslationResources = {
    [key: string]: string;
};
/**
 * Adaptador de API (Inyección de Dependencias).
 * Permite que el sistema de idiomas utilice TU propio servicio de red.
 */
export type LanguageApiAdapter = {
    /**
     * Debe retornar la lista completa de idiomas configurados en el sistema.
     * Se recomienda llamar al endpoint: GET /api/v1/langs
     */
    getLanguages: () => Promise<Language[]>;
    /**
     * Debe retornar el diccionario de traducciones para un idioma específico.
     * Se recomienda llamar al endpoint: GET /api/v1/langs/{codigo}
     * @param codigo El código del idioma a descargar
     */
    getTranslations: (codigo: string) => Promise<TranslationResources>;
};
/**
 * Motor de Traducción (Lógica de Negocio).
 * Encargado de buscar llaves y manejar fallbacks entre idiomas.
 */
export type TranslationEngine = {
    /**
     * Función principal para traducir.
     * Si la llave no existe, debe devolver la misma llave para no romper la UI.
     */
    translate: (key: string) => string;
    /** Carga un conjunto de traducciones en la memoria del motor */
    loadResources: (lang: string, resources: TranslationResources) => void;
    /** Cambia el idioma activo en el motor de forma instantánea */
    changeLanguage: (lang: string) => void;
    /** Retorna el código del idioma que el motor está usando actualmente */
    getCurrentLanguage: () => string;
};
/**
 * Modo de operación del sistema de traducciones en arquitecturas.
 * - `standalone`: Aplicación monolítica estándar (por defecto, retrocompatible).
 * - `shell`: Aplicación anfitriona en Microfrontends (orquesta y emite eventos).
 * - `mfe`: Microfrontend hijo (escucha eventos y prioriza caché).
 */
export type TranslationMode = 'standalone' | 'shell' | 'mfe';
/**
 * Propiedades del componente TranslationProvider.
 * Este es el componente raíz que habilita la internacionalización.
 */
export type TranslationProviderProps = {
    /** La aplicación que será envuelta por el contexto de traducción */
    children: ReactNode;
    /**
     * Modo de arquitectura del sistema.
     * @default 'standalone'
     */
    mode?: TranslationMode;
    /**
     * Nombre del CustomEvent global usado para sincronizar Shell y MFEs.
     * @default 'siesa:language-changed'
     */
    broadcastEventName?: string;
    /**
     * Adaptador para conectar con el backend.
     * Si no se provee, el sistema operará exclusivamente con recursos estáticos.
     */
    apiAdapter?: LanguageApiAdapter;
    /**
     * Lista de idiomas cargados localmente (Modo Desconectado).
     * Útil para prototipos o apps sin backend de idiomas.
     */
    staticLanguages?: Language[];
    /**
     * Diccionarios de traducción locales (Modo Desconectado).
     * Ejemplo: { es: { ... }, en: { ... } }
     */
    staticResources?: {
        [lang: string]: TranslationResources;
    };
    /**
     * Idioma inicial si no hay ninguno guardado en el navegador.
     * @default 'es'
     */
    defaultLanguage?: string;
    /**
     * Si es true, el sistema intentará inyectar el header Accept-Language en Axios automáticamente.
     * Requiere que se pase la 'axiosInstance'.
     * @default false
     */
    autoSetAxiosHeader?: boolean;
    /**
     * Instancia de Axios (o similar) donde se inyectará el header de idioma.
     */
    axiosInstance?: any;
};
//# sourceMappingURL=index.d.ts.map