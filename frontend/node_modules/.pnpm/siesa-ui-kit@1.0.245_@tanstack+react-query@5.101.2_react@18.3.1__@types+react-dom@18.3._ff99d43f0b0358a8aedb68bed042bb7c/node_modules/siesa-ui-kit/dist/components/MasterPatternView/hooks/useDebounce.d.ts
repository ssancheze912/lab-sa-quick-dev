/**
 * Returns a stable debounced function that delays calling `callback`
 * until `delay` ms of inactivity.
 *
 * @param callback - Function to debounce
 * @param delay - Debounce delay in milliseconds
 * @returns Object with `debouncedFn` and `cancel` to flush the pending timer
 */
export declare function useDebounce<T extends (...args: any[]) => void>(callback: T, delay: number): {
    debouncedFn: (...args: Parameters<T>) => void;
    cancel: () => void;
};
//# sourceMappingURL=useDebounce.d.ts.map