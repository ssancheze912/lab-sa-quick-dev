export interface WithholdingsConfirmModalProps {
    open: boolean;
    /** Título del diálogo. */
    title: string;
    /** Cuerpo / descripción. */
    body: string;
    /** Texto del botón de confirmación. */
    confirmLabel: string;
    /** Texto del botón de cancelar. */
    cancelLabel: string;
    /** `warning` para Recalcular, `danger` para Eliminar. @default 'warning' */
    type?: 'warning' | 'danger';
    /** Mutación en vuelo: bloquea botones y muestra spinner. */
    isLoading: boolean;
    onConfirm: () => void;
    onClose: () => void;
}
/**
 * Modal de confirmación genérico del panel de retenciones (FEAT-105-11.03).
 *
 * Envuelve el `AlertDialog` del kit; sirve para Recalcular (`warning`) y para
 * Eliminar (`danger`). Reutilizable por cualquier acción que requiera confirmar.
 */
export declare function WithholdingsConfirmModal({ open, title, body, confirmLabel, cancelLabel, type, isLoading, onConfirm, onClose, }: WithholdingsConfirmModalProps): import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=WithholdingsConfirmModal.d.ts.map