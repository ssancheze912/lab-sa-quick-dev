/**
 * Wrapper shim around `radix-ui` Popover.
 *
 * Exists so consuming code imports a stable symbol from `lib/ui/popover` —
 * insulating the FormCacheSelector from a future swap to a different popover
 * primitive (e.g. when Siesa UI Kit eventually ships one). It also gives unit
 * tests a single, easy `vi.mock` seam.
 *
 * `PopoverContent` wraps `PopoverPrimitive.Content` in an explicit
 * `PopoverPrimitive.Portal` so the panel is portalled to `document.body`.
 * Radix v1 does NOT do this automatically — without the Portal the panel
 * renders in-flow and stacking/z-index break.
 */
import * as React from 'react';
import { Popover as PopoverPrimitive } from 'radix-ui';
export declare const Popover: React.FC<PopoverPrimitive.PopoverProps>;
export declare const PopoverTrigger: React.ForwardRefExoticComponent<PopoverPrimitive.PopoverTriggerProps & React.RefAttributes<HTMLButtonElement>>;
export declare const PopoverAnchor: React.ForwardRefExoticComponent<PopoverPrimitive.PopoverAnchorProps & React.RefAttributes<HTMLDivElement>>;
export declare const PopoverPortal: React.FC<PopoverPrimitive.PopoverPortalProps>;
export declare const PopoverClose: React.ForwardRefExoticComponent<PopoverPrimitive.PopoverCloseProps & React.RefAttributes<HTMLButtonElement>>;
export declare const PopoverContent: React.ForwardRefExoticComponent<Omit<PopoverPrimitive.PopoverContentProps & React.RefAttributes<HTMLDivElement>, "ref"> & React.RefAttributes<HTMLDivElement>>;
//# sourceMappingURL=popover.d.ts.map