/**
 * Unit tests para `isFieldLocked` — el soporte declarativo de bloqueo de campos
 * por `lockedWhen` del MasterDocument (FEAT-104, historia 104-2.03).
 *
 * Caso real: en PURCHASE_RETURN ciertos campos de cabecera deben bloquearse
 * (candado + tooltip) una vez que el documento en DRAFT tiene líneas, para
 * evitar inconsistencias con los movimientos ya registrados.
 */
export {};
//# sourceMappingURL=fieldLock.test.d.ts.map