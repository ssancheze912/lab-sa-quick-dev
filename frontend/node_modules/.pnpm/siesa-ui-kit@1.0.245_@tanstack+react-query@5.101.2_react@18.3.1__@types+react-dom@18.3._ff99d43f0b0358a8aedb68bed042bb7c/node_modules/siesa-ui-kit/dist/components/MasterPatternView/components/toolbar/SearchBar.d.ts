export interface SearchBarProps {
    /** Callback fired with trimmed search value after debounce or Enter */
    onSearch: (value: string) => void;
    /** Initial search value (sourced from searchParams.search in the store) */
    initialValue?: string;
    /** Placeholder text */
    placeholder?: string;
}
/**
 * SearchBar with debounced input.
 * - Fires onSearch after SEARCH_DEBOUNCE_MS of inactivity with trimmed value
 * - Enter key fires immediately, cancelling any pending debounce
 * - × button clears the input and fires onSearch with empty string
 */
export declare function SearchBar({ onSearch, initialValue, placeholder, }: SearchBarProps): import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=SearchBar.d.ts.map