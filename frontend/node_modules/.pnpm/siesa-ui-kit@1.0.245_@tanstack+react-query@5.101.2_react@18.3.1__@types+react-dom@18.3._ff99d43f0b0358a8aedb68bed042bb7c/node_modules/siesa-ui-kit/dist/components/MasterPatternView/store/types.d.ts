/**
 * Store types for @siesa/master-pattern-view
 *
 * Defines the combined state shape for the Zustand store factory.
 * Each interface corresponds to one slice of the store.
 *
 * @internal
 * @module store/types
 */
import type { StoreApi } from 'zustand';
import type { MasterPatternViewEntity } from '../types/service.types';
import type { FormMode, PendingAction } from '../types/internal.types';
export interface SearchParams {
    page: number;
    pageSize: number;
    search: string;
    sortBy: string;
    sortOrder: 'asc' | 'desc';
    filters: ActiveFilter[];
}
export interface ListState<T extends MasterPatternViewEntity> {
    searchParams: SearchParams;
    /** Column-level filters (written by ListTable header inputs). Kept separate from
     * FilterPanel filters so each mechanism is independent — FilterPanel "Buscar"
     * no longer wipes active column filters, and column inputs survive FilterPanel
     * searches. Both are merged in useMasterList before calling service.getAll(). */
    columnFilters: ActiveFilter[];
    /** Quick-filter values written by the inline toolbar QuickFilters inputs.
     * Stored as { fieldName: rawStringValue }. Converted to ActiveFilter[] with
     * operator 'contains' in useMasterList before merging with the other filter
     * channels. Cleared by clearFilters() alongside columnFilters. */
    quickFilterValues: Record<string, string>;
    data: T[];
    total: number;
    isLoading: boolean;
    selectedIds: string[];
    hasSearched: boolean;
    setSearchParams: (params: Partial<SearchParams>) => void;
    setColumnFilters: (filters: ActiveFilter[]) => void;
    clearColumnFilters: () => void;
    setQuickFilterValues: (values: Record<string, string>) => void;
    clearQuickFilters: () => void;
    setData: (data: T[], total: number) => void;
    setIsLoading: (isLoading: boolean) => void;
    toggleSelection: (id: string) => void;
    clearSelection: () => void;
    setHasSearched: (hasSearched: boolean) => void;
}
export interface FormState<T extends MasterPatternViewEntity> {
    mode: FormMode;
    activeRecord: T | null;
    values: Record<string, unknown>;
    errors: Record<string, string>;
    isDirty: boolean;
    isSaving: boolean;
    activeTabKey: string;
    /** True while getById is in-flight after opening edit/view — blocks form interaction. */
    isLoadingRecord: boolean;
    /** True when getById failed — Guardar is disabled and an error banner is shown. */
    recordLoadFailed: boolean;
    openCreate: () => void;
    openEdit: (record: T) => void;
    openView: (record: T) => void;
    closeForm: () => void;
    /** Update activeRecord in place (e.g. after async getById resolves with company-context entity). */
    updateActiveRecord: (record: T) => void;
    setValues: (values: Record<string, unknown> | ((prev: Record<string, unknown>) => Record<string, unknown>)) => void;
    setErrors: (errors: Record<string, string>) => void;
    setIsDirty: (isDirty: boolean) => void;
    setIsSaving: (isSaving: boolean) => void;
    setActiveTabKey: (key: string) => void;
    setIsLoadingRecord: (loading: boolean) => void;
    setRecordLoadFailed: (failed: boolean) => void;
}
export interface ActiveFilter {
    field: string;
    operator: string;
    value: unknown;
}
export interface FilterState {
    isFilterPanelOpen: boolean;
    activeFilters: ActiveFilter[];
    activeFilterCount: number;
    toggleFilterPanel: () => void;
    setFilterPanelOpen: (open: boolean) => void;
    setActiveFilters: (filters: ActiveFilter[]) => void;
    clearFilters: () => void;
}
export interface UIState {
    isChangeStatusModalOpen: boolean;
    isCompanyEnableModalOpen: boolean;
    isNotEnabledModalOpen: boolean;
    isDirtyStateDialogOpen: boolean;
    isImportModalOpen: boolean;
    isConflictDialogOpen: boolean;
    pendingAction: PendingAction | null;
    openChangeStatusModal: () => void;
    closeChangeStatusModal: () => void;
    openCompanyEnableModal: () => void;
    closeCompanyEnableModal: () => void;
    openNotEnabledModal: () => void;
    closeNotEnabledModal: () => void;
    openDirtyStateDialog: () => void;
    closeDirtyStateDialog: () => void;
    openImportModal: () => void;
    closeImportModal: () => void;
    openConflictDialog: () => void;
    closeConflictDialog: () => void;
    setPendingAction: (action: PendingAction) => void;
    clearPendingAction: () => void;
    viewMode: 'table' | 'cards';
    setViewMode: (mode: 'table' | 'cards') => void;
}
export interface OverrideState {
    activeCompanyContext: string | undefined;
    overrides: Record<string, unknown>;
    globalValues: Record<string, unknown>;
    isShowGlobalValuesEnabled: boolean;
    /** Fields the user explicitly reverted to global in this editing session. */
    revertedFields: string[];
    setActiveCompanyContext: (context: string | undefined) => void;
    setOverrides: (overrides: Record<string, unknown>) => void;
    setGlobalValues: (values: Record<string, unknown>) => void;
    toggleShowGlobalValues: () => void;
    setRevertedFields: (fields: string[]) => void;
}
export interface MasterViewState<T extends MasterPatternViewEntity> extends ListState<T>, FormState<T>, FilterState, UIState, OverrideState {
}
export type MasterViewStore<T extends MasterPatternViewEntity> = StoreApi<MasterViewState<T>>;
//# sourceMappingURL=types.d.ts.map