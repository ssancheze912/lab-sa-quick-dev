import React from 'react';
import type { TranslationProviderProps, Language } from '../types';
/**
 * Interfaz del contexto interno de traducción
 */
interface TranslationContextType {
    translate: (key: string) => string;
    t: (key: string) => string;
    setLanguage: (lang: string) => Promise<void>;
    currentLanguage: string;
    languages: Language[];
    isReady: boolean;
    error: string | null;
}
/**
 * Provider principal para la internacionalización de Siesa
 */
export declare const TranslationProvider: React.FC<TranslationProviderProps>;
/**
 * Hook para acceder al contexto de traducción
 * Implementa una política de "Fallo Amistoso" (Graceful Fallback):
 * Si no hay un TranslationProvider en la raíz, no rompe la aplicación,
 * sino que devuelve un estado seguro por defecto.
 */
export declare const useTranslationInternal: () => TranslationContextType;
export {};
//# sourceMappingURL=TranslationProvider.d.ts.map