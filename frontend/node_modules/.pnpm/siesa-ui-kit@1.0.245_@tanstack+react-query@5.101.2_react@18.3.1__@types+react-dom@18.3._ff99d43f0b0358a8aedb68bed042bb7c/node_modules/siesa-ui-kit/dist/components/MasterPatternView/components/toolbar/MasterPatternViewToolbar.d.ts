import type { MasterPatternViewDefinition } from '../../types/definition.types';
import type { ActiveFilter } from '../../store/types';
export interface MasterPatternViewToolbarProps {
    definition: MasterPatternViewDefinition;
    /** Current free-text search value */
    searchValue?: string;
    /** Called when search text changes (debounced or Enter) */
    onSearch: (value: string) => void;
    /** Whether the advanced filter panel is open */
    isFilterPanelOpen: boolean;
    /** Called to toggle the filter panel */
    onToggleFilterPanel: () => void;
    /** Active filter values */
    activeFilters: ActiveFilter[];
    /** Active filter count badge */
    activeFilterCount: number;
    /** Called when user clicks Buscar in the filter panel */
    onApplyFilters: (filters: ActiveFilter[]) => void;
    /** Called when user clicks Limpiar in the filter panel */
    onClearFilters: () => void;
    /** Current quick filter values */
    quickFilterValues?: Record<string, string>;
    /** Called when a quick filter value changes */
    onQuickFilterChange?: (field: string, value: string) => void;
    /** Enable hierarchical view toggle (only shown when true) */
    hierarchical?: boolean;
    /** Current view mode */
    viewMode?: 'table' | 'tree';
    /** Called when user toggles view mode */
    onViewModeChange?: (mode: 'table' | 'tree') => void;
}
export declare function MasterPatternViewToolbar({ definition, searchValue, onSearch, isFilterPanelOpen, onToggleFilterPanel, activeFilters, activeFilterCount, onApplyFilters, onClearFilters, quickFilterValues, onQuickFilterChange, hierarchical, viewMode, onViewModeChange, }: MasterPatternViewToolbarProps): import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=MasterPatternViewToolbar.d.ts.map