/**
 * Detail tab types for master-detail sub-entity relationships.
 *
 * @module types/detail.types
 * @see FMPV-MVWC-E005-S001
 */
import type { MasterPatternViewEntity } from './service.types';
import type { MasterPatternViewFieldDefinition } from './definition.types';
export interface MasterDetailTabService<D extends MasterPatternViewEntity> {
    getAll: (masterId: string, companyId?: string) => Promise<D[]>;
    create: (record: Partial<D>, masterId: string, companyId?: string) => Promise<D>;
    update: (id: string, record: Partial<D>, masterId: string, companyId?: string) => Promise<D>;
    delete: (id: string, masterId: string, companyId?: string) => Promise<void>;
}
export interface MasterDetailTabConfig<D extends MasterPatternViewEntity> {
    key: string;
    label: string;
    entityName: string;
    fields: MasterPatternViewFieldDefinition[];
    service: MasterDetailTabService<D>;
    validate?: (values: Partial<D>) => Record<string, string>;
}
//# sourceMappingURL=detail.types.d.ts.map