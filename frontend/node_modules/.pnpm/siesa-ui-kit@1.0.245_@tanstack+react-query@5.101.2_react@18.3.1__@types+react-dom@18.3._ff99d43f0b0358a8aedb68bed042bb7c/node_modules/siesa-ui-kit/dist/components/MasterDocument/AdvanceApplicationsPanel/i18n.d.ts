/**
 * i18n del widget de Anticipos.
 *
 * Cumple CA-F8 (usa `useTranslation` de siesa-ui-kit) y CA-F9 (paridad ES/EN).
 * El motor de traducción del kit hace fallback al `key` cuando el host no ha
 * registrado las claves `advances.*`; por eso este módulo provee un diccionario
 * local que se usa como respaldo, garantizando español listo para usar sin
 * depender de que el documento consumidor registre los recursos.
 *
 * Las claves siguen el formato plano (no anidado) que espera
 * `DefaultTranslationEngine`.
 */
export declare const ADVANCES_TRANSLATIONS_ES: Record<string, string>;
export declare const ADVANCES_TRANSLATIONS_EN: Record<string, string>;
/** Diccionarios de respaldo por idioma, listos para registrar en el host si se desea. */
export declare const ADVANCES_TRANSLATIONS: {
    readonly es: Record<string, string>;
    readonly en: Record<string, string>;
};
/** Función de traducción con interpolación `{var}`. */
export type AdvancesT = (key: string, vars?: Record<string, string | number>) => string;
/**
 * Hook de traducción del widget.
 *
 * Usa `useTranslation` del kit; si la clave no está registrada en el host
 * (el motor devuelve la propia clave), cae al diccionario local en español.
 */
export declare function useAdvancesTranslation(): {
    t: AdvancesT;
};
//# sourceMappingURL=i18n.d.ts.map