/**
 * Unit tests para `resolveActionDisabledState` — el soporte declarativo de
 * "acción deshabilitada con motivo" del MasterDocument (FEAT-035 / FD-10).
 *
 * Caso real: en "Órdenes de Compra" el botón "Dar por cumplida" debe mostrarse
 * VISIBLE pero DESHABILITADO con un tooltip cuando el documento está en estado
 * RETAINED (el usuario debe liberar la retención primero).
 */
export {};
//# sourceMappingURL=actionVisibility.test.d.ts.map