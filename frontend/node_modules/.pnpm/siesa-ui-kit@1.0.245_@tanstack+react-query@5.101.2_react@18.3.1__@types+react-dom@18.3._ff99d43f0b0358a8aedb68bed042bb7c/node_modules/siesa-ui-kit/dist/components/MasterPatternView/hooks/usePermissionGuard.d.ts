import type { MasterPatternViewPermissions } from '../types/permissions.types';
import { type ToastCallback } from './useToast';
export interface PermissionGuard {
    isAllowed: boolean;
    showPermissionToast: () => void;
}
/**
 * Returns whether an operation is allowed and a function to show the
 * permission-denied toast if not.
 *
 * @param permissions - Current permission set
 * @param operation - The operation to check (e.g. 'CREATE', 'UPDATE')
 * @param masterType - The master entity type
 * @param externalToast - Optional consumer-provided toast callback
 */
export declare function usePermissionGuard(permissions: MasterPatternViewPermissions | undefined, operation: string, masterType: string, externalToast?: ToastCallback): PermissionGuard;
//# sourceMappingURL=usePermissionGuard.d.ts.map