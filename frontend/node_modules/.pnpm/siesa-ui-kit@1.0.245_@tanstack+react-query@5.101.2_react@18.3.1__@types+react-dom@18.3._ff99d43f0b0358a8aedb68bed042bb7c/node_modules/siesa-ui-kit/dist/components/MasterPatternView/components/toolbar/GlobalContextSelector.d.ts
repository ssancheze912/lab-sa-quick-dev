/**
 * GlobalContextSelector — toolbar context switcher for GLOBAL master types.
 *
 * Options: "Global" (always first) + all companies from props.
 * When activeContext is 'global', companyId is omitted from service calls.
 * UPDATE_GLOBAL permission guards the "Global" option.
 *
 * @module components/toolbar/GlobalContextSelector
 * @see FMPV-MVWC-E004-S003 I-001
 */
import React from 'react';
import type { MasterPatternViewCompany } from '../../types/service.types';
import type { MasterPatternViewPermissions } from '../../types/permissions.types';
export interface GlobalContextSelectorProps {
    companies: MasterPatternViewCompany[];
    activeContext: string;
    permissions?: MasterPatternViewPermissions;
    onContextChange: (context: string) => void;
}
export declare function GlobalContextSelector({ companies, activeContext, permissions, onContextChange, }: GlobalContextSelectorProps): React.ReactElement | null;
//# sourceMappingURL=GlobalContextSelector.d.ts.map