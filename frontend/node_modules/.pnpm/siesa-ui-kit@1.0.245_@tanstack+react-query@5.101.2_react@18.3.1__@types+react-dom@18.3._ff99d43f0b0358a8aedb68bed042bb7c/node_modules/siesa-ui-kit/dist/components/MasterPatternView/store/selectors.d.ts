/**
 * Typed selectors for the MasterView Zustand store.
 *
 * Pre-defined selectors for the most frequent read patterns.
 * Using named selectors avoids inline arrow functions in components,
 * which would create new references on every render and trigger unnecessary re-renders.
 *
 * @internal
 * @module store/selectors
 * @see FMPV-MVWC-E001-S003 R-002 — Typed selectors for frequent read patterns
 */
import type { MasterPatternViewEntity } from '../types/service.types';
import type { MasterViewState } from './types';
/** True when the form panel is visible (any non-closed mode). */
export declare const selectIsFormOpen: <T extends MasterPatternViewEntity>(state: MasterViewState<T>) => boolean;
/** The entity currently loaded in the form (null when form is closed or creating). */
export declare const selectActiveRecord: <T extends MasterPatternViewEntity>(state: MasterViewState<T>) => T | null;
/** True when the form has unsaved changes. */
export declare const selectIsDirty: <T extends MasterPatternViewEntity>(state: MasterViewState<T>) => boolean;
/** Current form mode. */
export declare const selectFormMode: <T extends MasterPatternViewEntity>(state: MasterViewState<T>) => import("../types/internal.types").FormMode;
/** Number of selected rows. */
export declare const selectSelectedCount: <T extends MasterPatternViewEntity>(state: MasterViewState<T>) => number;
/** True when at least one row is selected. */
export declare const selectHasSelection: <T extends MasterPatternViewEntity>(state: MasterViewState<T>) => boolean;
/** Selected row IDs array. */
export declare const selectSelectedIds: <T extends MasterPatternViewEntity>(state: MasterViewState<T>) => string[];
/** Current data array. */
export declare const selectData: <T extends MasterPatternViewEntity>(state: MasterViewState<T>) => T[];
/** True when a list fetch is in progress. */
export declare const selectIsLoading: <T extends MasterPatternViewEntity>(state: MasterViewState<T>) => boolean;
/** True when the filter panel is expanded. */
export declare const selectIsFilterPanelOpen: <T extends MasterPatternViewEntity>(state: MasterViewState<T>) => boolean;
/** Number of active filters. */
export declare const selectActiveFilterCount: <T extends MasterPatternViewEntity>(state: MasterViewState<T>) => number;
/** Active company context ('global' or a company ID). */
export declare const selectActiveCompanyContext: <T extends MasterPatternViewEntity>(state: MasterViewState<T>) => string | undefined;
//# sourceMappingURL=selectors.d.ts.map