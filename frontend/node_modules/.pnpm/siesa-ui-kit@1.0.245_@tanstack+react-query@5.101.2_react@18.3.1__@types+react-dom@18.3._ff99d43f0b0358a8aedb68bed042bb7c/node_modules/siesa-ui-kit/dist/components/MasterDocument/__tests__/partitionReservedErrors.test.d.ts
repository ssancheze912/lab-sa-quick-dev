/**
 * Unit tests para `partitionReservedErrors` — separa las claves reservadas
 * `_conflict` (HTTP 409) y `_forbidden` (HTTP 403) del resto de errores de campo
 * que devuelve `adapter.saveHeader` (FEAT-104, historia 104-2.03).
 *
 * Un `_conflict` debe disparar el flujo de conflicto (mismo modal que el polling),
 * NO renderizarse como un error de campo genérico vía SET_HEADER_ERRORS.
 */
export {};
//# sourceMappingURL=partitionReservedErrors.test.d.ts.map