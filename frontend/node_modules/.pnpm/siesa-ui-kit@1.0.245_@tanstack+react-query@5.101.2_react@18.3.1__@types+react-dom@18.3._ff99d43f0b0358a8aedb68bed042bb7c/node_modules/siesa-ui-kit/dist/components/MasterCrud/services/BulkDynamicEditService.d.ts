export interface BulkUpdatePayload {
    recordIds: string[];
    entityPatches: Array<{
        entityCode: string;
        fieldValues: Record<string, unknown>;
    }>;
}
export interface BulkProgressEvent {
    processed: number;
    total: number;
    succeeded: number;
    failed: number;
    isComplete: boolean;
    errors?: Array<{
        recordId: string;
        reason: string;
    }>;
}
export declare class BulkDynamicEditService {
    private baseUrl;
    constructor(baseUrl: string);
    bulkUpdate(featureCode: string, payload: BulkUpdatePayload, onProgress: (event: BulkProgressEvent) => void, signal?: AbortSignal): Promise<void>;
}
//# sourceMappingURL=BulkDynamicEditService.d.ts.map