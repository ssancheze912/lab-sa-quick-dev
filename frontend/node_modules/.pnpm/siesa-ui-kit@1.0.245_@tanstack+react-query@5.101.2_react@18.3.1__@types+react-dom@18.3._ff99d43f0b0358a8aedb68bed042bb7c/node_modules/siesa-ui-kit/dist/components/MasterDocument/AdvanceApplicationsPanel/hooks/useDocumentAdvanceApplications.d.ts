import type { AdvancesHttpRequest, DocumentAdvanceApplicationDto } from '../types';
export interface UseDocumentAdvanceApplicationsParams {
    documentId: string;
    /** Endpoint GET de aplicaciones del documento (`/documents/{id}/advances`). */
    applicationsEndpoint: string;
    httpRequest: AdvancesHttpRequest;
    enabled?: boolean;
}
/**
 * Lista las aplicaciones de anticipo del documento (lo que el panel renderiza).
 *
 * Cache key: `['advance-applications', documentId]`.
 * Normaliza el `status` (int o string) del backend a la unión del widget.
 */
export declare function useDocumentAdvanceApplications({ documentId, applicationsEndpoint, httpRequest, enabled, }: UseDocumentAdvanceApplicationsParams): import("@tanstack/react-query").UseQueryResult<DocumentAdvanceApplicationDto[], Error>;
//# sourceMappingURL=useDocumentAdvanceApplications.d.ts.map