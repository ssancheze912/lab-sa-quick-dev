/**
 * i18n del widget de Retenciones (FEAT-105-11).
 *
 * Usa `useTranslation` de siesa-ui-kit; si el host no registró las claves
 * `withholdings.*` (el motor devuelve la propia clave), cae al diccionario local
 * en español. Garantiza español listo para usar sin depender de que el documento
 * consumidor registre los recursos. Las claves siguen el formato plano que espera
 * `DefaultTranslationEngine`.
 */
export declare const WITHHOLDINGS_TRANSLATIONS_ES: Record<string, string>;
export declare const WITHHOLDINGS_TRANSLATIONS_EN: Record<string, string>;
/** Diccionarios de respaldo por idioma, listos para registrar en el host si se desea. */
export declare const WITHHOLDINGS_TRANSLATIONS: {
    readonly es: Record<string, string>;
    readonly en: Record<string, string>;
};
/** Función de traducción con interpolación `{var}`. */
export type WithholdingsT = (key: string, vars?: Record<string, string | number>) => string;
/**
 * Hook de traducción del widget.
 *
 * Usa `useTranslation` del kit; si la clave no está registrada en el host
 * (el motor devuelve la propia clave), cae al diccionario local en español.
 */
export declare function useWithholdingsTranslation(): {
    t: WithholdingsT;
};
//# sourceMappingURL=i18n.d.ts.map