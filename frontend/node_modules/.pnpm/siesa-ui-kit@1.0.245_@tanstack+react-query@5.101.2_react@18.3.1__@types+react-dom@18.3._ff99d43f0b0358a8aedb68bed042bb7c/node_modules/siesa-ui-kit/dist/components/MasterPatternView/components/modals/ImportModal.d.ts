/**
 * ImportModal — XLSX file import with drag-and-drop, validation, and error summary.
 *
 * Features:
 * - Only accepts .xlsx files (inline error for others)
 * - Shows filename + size after valid file selection
 * - "Importar" button calls onImport(file); "Cancelar" calls onClose
 * - Shows error summary table when importErrors is provided (modal stays open)
 *
 * @module components/modals/ImportModal
 * @see FMPV-MVWC-E005-S003
 */
import React from 'react';
import type { MasterPatternViewImportError } from '../../types/service.types';
export interface ImportModalProps {
    isOpen: boolean;
    onClose: () => void;
    onImport: (file: File) => void;
    isLoading: boolean;
    importErrors?: MasterPatternViewImportError[];
}
export declare function ImportModal({ isOpen, onClose, onImport, isLoading, importErrors, }: ImportModalProps): React.ReactElement | null;
//# sourceMappingURL=ImportModal.d.ts.map