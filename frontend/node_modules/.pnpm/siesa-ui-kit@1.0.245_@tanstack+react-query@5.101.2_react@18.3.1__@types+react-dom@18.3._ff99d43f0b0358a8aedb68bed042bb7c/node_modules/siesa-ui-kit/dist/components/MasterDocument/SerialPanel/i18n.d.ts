/**
 * i18n del SerialPanel (soporte nativo de seriales en MasterDocument).
 *
 * Sigue el mismo patrón que el resto del kit: usa `useTranslation`; si la clave
 * no está registrada en el host (el motor devuelve la propia clave), cae al
 * diccionario local en español. Claves en formato plano (no anidado).
 */
export declare const SERIAL_PANEL_TRANSLATIONS_ES: Record<string, string>;
export declare const SERIAL_PANEL_TRANSLATIONS_EN: Record<string, string>;
/** Diccionarios de respaldo por idioma, listos para registrar en el host si se desea. */
export declare const SERIAL_PANEL_TRANSLATIONS: {
    readonly es: Record<string, string>;
    readonly en: Record<string, string>;
};
/** Función de traducción con interpolación `{var}`. */
export type SerialPanelT = (key: string, vars?: Record<string, string | number>) => string;
/**
 * Hook de traducción del SerialPanel. Usa `useTranslation` del kit; si la clave
 * no está registrada en el host, cae al diccionario local en español.
 */
export declare function useSerialPanelTranslation(): {
    t: SerialPanelT;
};
//# sourceMappingURL=i18n.d.ts.map