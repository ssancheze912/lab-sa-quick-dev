import type { MasterDocumentProps } from './MasterDocument.types';
/**
 * MasterDocument — componente de presentación que compone el documento maestro
 * completo: barra de acciones, tira de contexto, encabezado, líneas y FactBox.
 *
 * Es un componente puramente presentacional: recibe todos los datos y callbacks
 * como props y no contiene lógica de negocio. Para la versión con lógica
 * completa, usar `SmartMasterDocument` dentro de un `MasterDocumentProvider`.
 *
 * @see SmartMasterDocument — versión inteligente con template y adaptador
 * @see MasterDocumentProvider — proveedor de contexto para el modo smart
 *
 * @example
 * ```tsx
 * <MasterDocument
 *   documentId="FA-001"
 *   docTypeBadge="Factura"
 *   docState="draft"
 *   mode="edit"
 *   headerFields={fields}
 *   headerValues={values}
 *   lines={lines}
 *   lineColumns={columns}
 *   onHeaderFieldChange={handleChange}
 *   onLineSave={handleSave}
 *   onDeleteLine={handleDelete}
 * />
 * ```
 */
export declare function MasterDocument({ documentId, documentNumber, docTypeBadge, breadcrumbLabel, onBreadcrumbClick, docState, stateBadge, transitions, onTransition, mode, actions, contextItems, currencyPanel, headerLayout, headerFields, headerValues, headerErrors, onHeaderFieldChange, lookupConfigs, fetcher, onRecordSelect, linesTitle, lines, lineColumns, lineFields, lineComputedRules, lineServerRules, onEvaluateServerRule, lineWidgetSlot, onLineFormValuesChange, lineModalSections, serialSupport, serialReadOnly, validateSerial, fetchAvailableSerials, lineAlerts, canAddLine, onAddLine, onEditLine, onViewLine, onDeleteLine, onDuplicateLine, lineEditable, onLineSave, factBoxSections, conflict, forbidden, crossing, loading, busy, className, }: MasterDocumentProps): import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=MasterDocument.d.ts.map