/**
 * AssignmentBadge — building2 badge for records linked to a company (GLOBAL type).
 *
 * Renders only when isAssigned=true. Shows Building2 icon with company name tooltip.
 * Badge colors per UX spec §5.3: bg-blue-50 / border-blue-300 / text-blue-700.
 *
 * @module components/list/AssignmentBadge
 * @see FMPV-MVWC-E004-S003 I-002
 */
import React from 'react';
export interface AssignmentBadgeProps {
    isAssigned: boolean;
    companyName: string;
}
export declare function AssignmentBadge({ isAssigned, companyName }: AssignmentBadgeProps): React.ReactElement | null;
//# sourceMappingURL=AssignmentBadge.d.ts.map