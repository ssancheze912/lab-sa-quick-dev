import type { ParameterCategory, ParameterDefinition, ParameterChange, ParameterSavePayload } from './ParameterEditor.types';
/**
 * Returns all parameters from a category (direct + from sections).
 */
export declare function getAllCategoryParameters(category: ParameterCategory): ParameterDefinition[];
/**
 * Builds the initial values map from dataSource.
 * Maps parameterId -> resolvedValue
 */
export declare function buildOriginalValues(dataSource: ParameterCategory[]): Map<string, string>;
/**
 * Validates a parameter value against its validation rules.
 * Returns an error message if invalid, or empty string if valid.
 */
export declare function validateParameter(param: ParameterDefinition, value: string): string;
/**
 * Filters parameters by search term.
 * Matches against name, code, and description (case-insensitive).
 */
export declare function filterParameters(parameters: ParameterDefinition[], searchTerm: string): ParameterDefinition[];
/**
 * Returns visible parameters sorted by sortOrder.
 */
export declare function getVisibleParameters(parameters: ParameterDefinition[]): ParameterDefinition[];
/**
 * Gets the count of changes for a specific category.
 */
export declare function getCategoryChangeCount(category: ParameterCategory, editedValues: Map<string, string>, originalValues: Map<string, string>): number;
/**
 * Gets the total count of all changes across dataSource.
 */
export declare function getTotalChangeCount(editedValues: Map<string, string>, originalValues: Map<string, string>): number;
/**
 * Builds the list of changes for the confirm panel.
 */
export declare function buildChangeList(dataSource: ParameterCategory[], editedValues: Map<string, string>, originalValues: Map<string, string>, entityDisplayLabels?: Map<string, string>): ParameterChange[];
/**
 * Filters a category by search term, including sections.
 * Returns null if no parameters match.
 */
export declare function filterCategoryBySearch(category: ParameterCategory, searchTerm: string): ParameterCategory | null;
/**
 * Builds the save payload from the change list.
 */
export declare function buildSavePayload(changes: ParameterChange[]): ParameterSavePayload[];
/**
 * Returns the display label for a value.
 * For BOOLEAN returns "Sí"/"No", for LIST returns the option label, for sensitive returns masked.
 */
export declare function getDisplayValue(value: string, dataType: string, isSensitive: boolean, listOptions?: Array<{
    value: string;
    label: string;
}> | null, entityDisplayLabel?: string): string;
/**
 * Gets the current value for a parameter (edited or original).
 */
export declare function getCurrentValue(paramId: string, editedValues: Map<string, string>, originalValues: Map<string, string>): string;
/**
 * Extracts min/max values from validation rules.
 */
export declare function getMinMaxFromValidations(validations: ParameterDefinition['validations']): {
    min?: number;
    max?: number;
    maxLength?: number;
};
//# sourceMappingURL=ParameterEditor.utils.d.ts.map