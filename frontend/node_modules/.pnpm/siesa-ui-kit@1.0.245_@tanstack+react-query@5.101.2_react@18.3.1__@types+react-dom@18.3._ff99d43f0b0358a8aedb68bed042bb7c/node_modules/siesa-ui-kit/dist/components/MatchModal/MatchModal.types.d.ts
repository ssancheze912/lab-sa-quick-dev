import type { ReactNode } from 'react';
/**
 * Resultado de una coincidencia encontrada
 */
export interface MatchResult {
    /** Identificador único del registro coincidente */
    id: string | number;
    /** Nombre o descripción principal del registro */
    name: string;
    /** Otros datos adicionales que puedan ser necesarios */
    [key: string]: any;
}
/**
 * Propiedades del componente MatchModal
 */
export interface MatchModalProps {
    /** Si el modal está abierto */
    isOpen: boolean;
    /** Callback al cerrar el modal (X o cancelar) */
    onClose: () => void;
    /** Lista de coincidencias encontradas */
    matches: MatchResult[];
    /** Nombre del campo donde se encontró la coincidencia (ej: "Nombre", "Código") */
    fieldName: string;
    /** Callback al hacer clic en "Ver" una coincidencia específica */
    onView: (match: MatchResult) => void;
    /** Callback al hacer clic en "Continuar creando" */
    onContinue: () => void;
    /** Título personalizado del modal (opcional) */
    title?: ReactNode;
    /** Clases CSS adicionales para el contenedor */
    className?: string;
}
//# sourceMappingURL=MatchModal.types.d.ts.map