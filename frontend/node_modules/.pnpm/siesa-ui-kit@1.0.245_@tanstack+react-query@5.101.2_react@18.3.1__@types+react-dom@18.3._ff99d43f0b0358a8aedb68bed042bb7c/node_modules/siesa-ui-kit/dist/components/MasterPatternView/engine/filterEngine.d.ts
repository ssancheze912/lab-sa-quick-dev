/**
 * Filter Engine — Builds filter queries from active filter state.
 *
 * Pure functions. No React hooks, no side effects.
 *
 * @module filterEngine
 * @see FMPV-MVWC-E001-S004
 */
import type { MasterPatternViewFieldDefinition, FilterOperator } from '../types';
/**
 * Returns the default filter operator for a given data type.
 *
 * - `string` / `guid` → `'contains'` (text search)
 * - All others → `'equals'` (exact match)
 */
export declare function getDefaultOperator(dataType: string): FilterOperator;
/**
 * Builds a filter query object from active filter values.
 *
 * Skips empty strings, undefined, and null values.
 * Each entry maps to `{ value, operator }` for the service layer.
 *
 * @param activeFilters - Current filter values keyed by field name
 * @param fields - Field definitions to resolve data types and operators
 * @returns A query object suitable for passing to `service.getAll({ filters })`
 */
export declare function buildFilterQuery(activeFilters: Record<string, unknown>, fields: MasterPatternViewFieldDefinition[]): Record<string, unknown>;
//# sourceMappingURL=filterEngine.d.ts.map