/**
 * TreeView — hierarchical tree with lazy-loaded children and per-node actions.
 *
 * Features:
 * - Calls service.getTree(null, 1) on mount for root nodes
 * - Per-node expand: calls service.getChildren(nodeId, companyId?) — cached via TanStack Query
 * - Inline expand/collapse toggle; expansion state is session-local (cleared on unmount)
 * - "+ Hijo" button: calls onCreateChild with the parent node; disabled at max depth
 * - "Editar" button: calls onEdit with the node
 * - DnD via @dnd-kit when onMoveNode prop provided
 *
 * @module components/list/TreeView
 * @see FMPV-MVWC-E005-S002
 */
import React from 'react';
import type { MasterPatternViewDefinition } from '../../types/definition.types';
import type { MasterPatternViewEntity } from '../../types/service.types';
import type { MasterPatternViewTreeNode, MasterPatternViewTreeService } from '../../types/tree.types';
export interface TreeViewProps<T extends MasterPatternViewEntity> {
    service: MasterPatternViewTreeService<T> & {
        getAll?: (params?: any) => Promise<T[]>;
        getById?: (id: string) => Promise<T>;
        create?: (record: Partial<T>) => Promise<T>;
        update?: (id: string, record: Partial<T>) => Promise<T>;
        delete?: (id: string) => Promise<void>;
        changeStatus?: (id: string, isActive: boolean) => Promise<T>;
    };
    definition: MasterPatternViewDefinition;
    hierarchicalDepth?: number;
    companyId?: string;
    onEdit: (node: MasterPatternViewTreeNode<T>) => void;
    onCreateChild: (parentNode: MasterPatternViewTreeNode<T>) => void;
    onMoveNode?: (id: string, newParentId: string | null) => void;
}
export declare function TreeView<T extends MasterPatternViewEntity>({ service, definition, hierarchicalDepth, companyId, onEdit, onCreateChild, onMoveNode: _onMoveNode, }: TreeViewProps<T>): React.ReactElement;
//# sourceMappingURL=TreeView.d.ts.map