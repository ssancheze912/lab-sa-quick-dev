import type { TreeNode } from '../../Tree.types';
/**
 * Simple tree: 1 root with 2 leaf children.
 *
 * root
 * ├── child-1
 * └── child-2
 */
export declare const simpleTree: TreeNode[];
/**
 * Mixed tree: parent with one checked child and one disabled child.
 * Used to test indeterminate parent state and disabled cascade skipping.
 *
 * parent
 * ├── enabled-child (checked)
 * └── disabled-child (unchecked, disabled)
 */
export declare const mixedTree: TreeNode[];
/**
 * Deep tree: 4 levels deep.
 *
 * level-0
 * └── level-1
 *     └── level-2
 *         └── level-3
 */
export declare const deepTree: TreeNode[];
/**
 * Single node tree: 1 leaf with no children.
 */
export declare const singleNode: TreeNode[];
/**
 * Empty tree: no nodes at all.
 */
export declare const emptyTree: TreeNode[];
/**
 * Tree with all descendants disabled.
 * Used to test cascade when all targets are disabled.
 *
 * root-all-disabled
 * ├── disabled-a (disabled)
 * └── disabled-b (disabled)
 */
export declare const allDisabledTree: TreeNode[];
/**
 * Tree with multiple roots for testing flattenTree and getVisibleNodes.
 *
 * root-a
 * ├── a-child-1
 * └── a-child-2
 * root-b
 * └── b-child-1
 */
export declare const multiRootTree: TreeNode[];
/**
 * Tree with data payloads for testing generic type preservation.
 */
export interface TestPayload {
    code: string;
    priority: number;
}
export declare const dataTree: TreeNode<TestPayload>[];
//# sourceMappingURL=trees.d.ts.map