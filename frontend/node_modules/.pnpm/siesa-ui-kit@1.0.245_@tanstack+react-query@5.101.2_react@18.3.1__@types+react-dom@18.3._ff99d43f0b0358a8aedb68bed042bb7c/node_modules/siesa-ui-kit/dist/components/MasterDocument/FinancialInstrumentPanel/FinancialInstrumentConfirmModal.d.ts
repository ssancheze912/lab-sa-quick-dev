export interface FinancialInstrumentConfirmModalProps {
    open: boolean;
    /** Título del diálogo. */
    title: string;
    /** Cuerpo / descripción. */
    body: string;
    /** Texto del botón de confirmación. */
    confirmLabel: string;
    /** Texto del botón de cancelar. */
    cancelLabel: string;
    /** Mutación en vuelo: bloquea botones y muestra spinner. */
    isLoading: boolean;
    onConfirm: () => void;
    onClose: () => void;
}
/**
 * Modal de confirmación de borrado del Instrumento Financiero (105-17).
 *
 * Envuelve el `AlertDialog` del kit (variante `danger`); el borrado es lógico
 * (soft-delete) y libera el slot para capturar uno nuevo.
 */
export declare function FinancialInstrumentConfirmModal({ open, title, body, confirmLabel, cancelLabel, isLoading, onConfirm, onClose, }: FinancialInstrumentConfirmModalProps): import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=FinancialInstrumentConfirmModal.d.ts.map