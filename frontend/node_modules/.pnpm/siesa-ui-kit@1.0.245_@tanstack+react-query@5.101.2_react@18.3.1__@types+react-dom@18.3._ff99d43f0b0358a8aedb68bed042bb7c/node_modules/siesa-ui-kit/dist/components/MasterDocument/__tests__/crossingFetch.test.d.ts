/**
 * Tests del helper de salida HTTP del panel de cruce (Modelo B — FEAT-094).
 *
 * Cubre el refactor que enruta el cruce por `adapter.genericFetch` en lugar de
 * `fetch` directo a `baseUrl`:
 *  1. createCrossingFetch PREFIERE adapter.genericFetch (auth/baseUrl/remapeo).
 *  2. Sin genericFetch → cae a fetch directo contra baseUrl (Storybook/demo).
 *  3. El fallback normaliza errores HTTP a Error con errorCode/data (FE-08).
 *  4. extractCrossingError lee errorCode/message de las formas comunes.
 *  5. isLineLevelCrossingError clasifica BR-01/11/16 como inline.
 */
export {};
//# sourceMappingURL=crossingFetch.test.d.ts.map