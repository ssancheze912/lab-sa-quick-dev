import type { Contact } from './contact.types';
import type { Fetcher } from '@components/LookupField';
export interface CatalogItem {
    id: string;
    name: string;
}
export interface LookupEntityConfig {
    entity: string;
    displayFields: string[];
    searchFields?: string[];
    displayTemplate?: string;
}
export interface LookupConfig {
    fetcher: Fetcher;
    socialNetworkTypes: LookupEntityConfig;
    countries: LookupEntityConfig;
    states?: LookupEntityConfig;
    cities?: LookupEntityConfig;
    neighborhoods?: LookupEntityConfig;
}
export interface IContactServiceAdapter {
    getByRecordId(recordId: string): Promise<Contact[]>;
    save(recordId: string, contacts: Contact[]): Promise<void>;
    getClassifications?(): Promise<number[]>;
    lookupConfig: LookupConfig;
}
//# sourceMappingURL=adapter.types.d.ts.map