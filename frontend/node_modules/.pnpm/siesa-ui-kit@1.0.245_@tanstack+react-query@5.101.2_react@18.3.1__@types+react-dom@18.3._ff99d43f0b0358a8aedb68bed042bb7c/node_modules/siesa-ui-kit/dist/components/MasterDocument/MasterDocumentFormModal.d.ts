import type { MasterDocumentFormModalProps } from './MasterDocument.types';
/**
 * MasterDocumentFormModal — modal para crear o editar una línea del documento.
 *
 * Soporta dos layouts:
 * - Sin `sections`: grid plano de campos (comportamiento original).
 * - Con `sections`: campos agrupados en secciones verticales. Cada sección puede
 *   ser flat (collapsible: false) o tipo acordeón expandible (collapsible: true).
 *
 * @example
 * ```tsx
 * <MasterDocumentFormModal
 *   open={open}
 *   onClose={onClose}
 *   onConfirm={handleSave}
 *   fields={lineFields}
 *   title="Nueva línea"
 *   sections={[
 *     { key: "item",         label: "Datos de la línea", defaultOpen: true },
 *     { key: "segmentacion", label: "Segmentación",      defaultOpen: false, collapsible: true },
 *   ]}
 * />
 * ```
 */
export declare function MasterDocumentFormModal({ open, onClose, onConfirm, onConfirmAndAddAnother, fields, initialData, computedRules, serverRules, onEvaluateServerRule, errors: externalErrors, title, confirmLabel, cancelLabel, docState, columns, sections, widgetSlot, onFormValuesChange, className, }: MasterDocumentFormModalProps): import("react/jsx-runtime").JSX.Element | null;
//# sourceMappingURL=MasterDocumentFormModal.d.ts.map