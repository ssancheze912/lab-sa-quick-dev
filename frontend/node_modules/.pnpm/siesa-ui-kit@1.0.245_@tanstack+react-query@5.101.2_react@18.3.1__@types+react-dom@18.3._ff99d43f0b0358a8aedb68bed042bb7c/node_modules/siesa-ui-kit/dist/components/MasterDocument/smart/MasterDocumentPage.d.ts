export interface MasterDocumentPageProps {
    /** Clase del documento. Fallback: template.identity.documentClass */
    documentClass?: string;
    /** Compañía del contexto del usuario (obligatorio) */
    companyId: string;
    /** Centro de operación del contexto del usuario (obligatorio) */
    operationCenterId: string;
    /** Si se pasa, arranca directo en el documento (edit/view). Si no, en el listado. */
    documentId?: string;
}
/**
 * MasterDocumentPage — Punto de entrada único por clase de documento.
 *
 * Recibe la compañía y centro de operación del contexto del usuario.
 * El wizard solo pide el tipo de documento — compañía y CO ya están seteados.
 *
 * @example
 * ```tsx
 * <MasterDocumentProvider template={pedidoVentaTemplate} adapter={adapter}>
 *   <MasterDocumentPage
 *     companyId="C001"
 *     operationCenterId="CO001"
 *   />
 * </MasterDocumentProvider>
 * ```
 */
export declare function MasterDocumentPage({ documentClass, companyId, operationCenterId, documentId, }: MasterDocumentPageProps): import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=MasterDocumentPage.d.ts.map