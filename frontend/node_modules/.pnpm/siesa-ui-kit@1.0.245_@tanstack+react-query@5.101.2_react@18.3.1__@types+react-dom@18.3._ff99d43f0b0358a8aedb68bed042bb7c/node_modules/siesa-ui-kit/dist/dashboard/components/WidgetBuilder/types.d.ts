import type { WidgetDefinition, WidgetPresentationType, DomainCategory } from '../../types';
export interface WizardFormData extends Partial<WidgetDefinition> {
    nombre: string;
    descripcion: string;
    categoria: DomainCategory;
    tipo: WidgetPresentationType;
    icono: string;
    roles_permitidos: string[];
    col_min: number;
    col_max: number;
    cols_default: number;
    altura_default: number;
    activo: boolean;
    endpoint_url: string;
    endpoint_method: 'GET' | 'POST';
    endpoint_headers: Record<string, string>;
    endpoint_params: Record<string, string | number | boolean>;
    refresh_policy: number;
    es_privado: boolean;
    is_internal: boolean;
    /** Modo de selección de fuente de datos */
    catalog_mode: 'catalogo' | 'manual';
    /** ID del indicador seleccionado del catálogo */
    catalog_indicator_id?: string;
    /** API Key para indicadores que la requieren (ej: FRED) */
    catalog_api_key: string;
    /** Path al campo valor en la respuesta (pre-fill desde catálogo) */
    campo_valor: string;
    /** Path al campo fecha en la respuesta (pre-fill desde catálogo) */
    campo_fecha: string;
}
export interface WizardState {
    formData: WizardFormData;
    testSuccess: boolean;
    isTesting: boolean;
    jsonPreview: Record<string, unknown>[] | null;
    mappingX: string;
    mappingY: string;
}
export declare const INITIAL_FORM_DATA: WizardFormData;
//# sourceMappingURL=types.d.ts.map