import type { LayoutUpdatePayload, UserWidgetPreference, DashboardFetcher } from '../types';
/**
 * GET /api/dashboard/layout
 * Carga el layout guardado para el usuario autenticado.
 */
export declare const getUserLayout: (userId: string, fetcher: DashboardFetcher) => Promise<UserWidgetPreference[]>;
/**
 * PUT /api/dashboard/layout
 * Reemplaza el layout completo del usuario (llamado con debounce 800ms).
 */
export declare const saveUserLayout: (payload: LayoutUpdatePayload, fetcher: DashboardFetcher) => Promise<{
    success: boolean;
}>;
//# sourceMappingURL=dashboardLayout.d.ts.map