import type { WidgetDefinition, UserWidgetPreference, DashboardPeriod, DashboardFetcher } from '../types';
interface UseWidgetEngineProps {
    userId: string;
    userRoles: string[];
    fetcher: DashboardFetcher;
}
/**
 * Motor central de orquestación.
 * Obtiene el Catálogo de Widgets disponibles para el usuario y el Layout guardado.
 * Gestiona el estado global de la vista (periodo de fecha actual y modo de edición).
 */
export declare const useWidgetEngine: ({ userId, userRoles, fetcher }: UseWidgetEngineProps) => {
    catalog: WidgetDefinition[];
    initialLayout: UserWidgetPreference[];
    isLoading: boolean;
    initError: string | null;
    isEditMode: boolean;
    toggleEditMode: () => void;
    period: DashboardPeriod;
    setPeriod: (newPeriod: DashboardPeriod) => void;
    getWidgetDefinition: (widgetId: string) => WidgetDefinition | undefined;
    addToCatalog: (widget: WidgetDefinition) => void;
    updateInCatalog: (updated: WidgetDefinition) => void;
    removeFromCatalog: (widgetId: string) => void;
};
export {};
//# sourceMappingURL=useWidgetEngine.d.ts.map