import type { FetchProxyPayload, DashboardFetcher, SchemaField } from '../types';
export interface EndpointMeta {
    generatedAt?: string;
    period?: string;
    rowCount?: number;
}
export interface ProxyFetchResult {
    data: unknown;
    schema: {
        fields: SchemaField[];
    } | null;
    endpointMeta: EndpointMeta | null;
}
/**
 * Solicita datos de un widget al Proxy del backend.
 * Retorna data + schema (si el endpoint devolvió envelope estándar).
 */
export declare const fetchWidgetDataProxy: (payload: FetchProxyPayload, fetcher: DashboardFetcher) => Promise<ProxyFetchResult>;
/**
 * Invalida la caché del proxy para un widget.
 * DELETE /api/dashboard/proxy/cache/:widgetId
 */
export declare const invalidateWidgetCache: (widgetId: string, fetcher: DashboardFetcher) => Promise<void>;
//# sourceMappingURL=dashboardProxy.d.ts.map