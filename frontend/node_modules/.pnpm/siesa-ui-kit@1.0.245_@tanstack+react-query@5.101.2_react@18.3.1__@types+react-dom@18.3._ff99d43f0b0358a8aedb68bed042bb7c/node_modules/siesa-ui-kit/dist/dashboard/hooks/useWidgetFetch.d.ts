import type { DashboardPeriod, SchemaField } from '../types';
import type { EndpointMeta } from '../api/dashboardProxy';
interface UseWidgetFetchResult<T> {
    data: T | null;
    schema: {
        fields: SchemaField[];
    } | null;
    meta: EndpointMeta | null;
    loading: boolean;
    error: string | null;
}
/**
 * Hook centralizado para fetching de datos de widgets.
 * Retorna data (post field_mapping) + schema del endpoint (si disponible).
 */
export declare function useWidgetFetch<T>(widgetId: string, period: DashboardPeriod, refreshPolicy?: number, fieldMapping?: Record<string, string>): UseWidgetFetchResult<T>;
export {};
//# sourceMappingURL=useWidgetFetch.d.ts.map