import type { WidgetDefinition, DashboardFetcher, AllowedInternalRoutes, DataSourceOption } from '../types';
/**
 * GET /api/dashboard/widgets
 * Devuelve el catálogo de widgets filtrado por el rol del usuario (vía JWT en el host).
 */
export declare const getCatalogWidgets: (fetcher: DashboardFetcher) => Promise<WidgetDefinition[]>;
/**
 * POST /api/dashboard/widgets
 * Registra un widget creado desde el Widget Builder.
 */
export declare const createWidget: (newWidget: Omit<WidgetDefinition, "version">, fetcher: DashboardFetcher) => Promise<WidgetDefinition>;
/**
 * PATCH /api/dashboard/widgets/:widgetId
 * Actualiza un widget existente (campos parciales).
 */
export declare const updateWidget: (widgetId: string, patch: Partial<WidgetDefinition>, fetcher: DashboardFetcher) => Promise<WidgetDefinition>;
/**
 * DELETE /api/dashboard/widgets/:widgetId
 * Elimina un widget del catalogo (requiere rol admin).
 */
export declare const deleteWidget: (widgetId: string, fetcher: DashboardFetcher) => Promise<{
    deleted: boolean;
}>;
/**
 * GET /api/dashboard/widgets/allowed-internal-routes
 * Retorna la configuracion publica del whitelist para el Widget Builder.
 */
export declare const getAllowedInternalRoutes: (fetcher: DashboardFetcher) => Promise<AllowedInternalRoutes>;
/**
 * GET /api/dashboard/widgets/data-sources
 * Retorna el catalogo de fuentes internas filtrado por roles del usuario.
 */
export declare const getDataSources: (fetcher: DashboardFetcher) => Promise<DataSourceOption[]>;
//# sourceMappingURL=dashboardCatalog.d.ts.map