import type { WidgetProps, ChartDataPoint } from '../types';
export interface ChartDataResult {
    chartData: Record<string, unknown>[];
    valueKeys: string[];
    categoryField: string;
    aliases: Record<string, string>;
    data: ChartDataPoint[] | null;
    loading: boolean;
    error: string | null;
}
/**
 * Hook centralizado para resolución de datos en widgets de gráficas.
 * Elimina el useMemo duplicado en ChartLine, ChartVentasMensual, ChartBarHorizontal y ChartDonut.
 *
 * Resuelve: field_mapping → userOverride → config_schema → heurística (4 capas).
 * Propaga aliases para labels de leyenda y tooltip.
 */
export declare function useChartData(definition: WidgetProps['definition'], preference: WidgetProps['preference'], period: WidgetProps['period']): ChartDataResult;
//# sourceMappingURL=useChartData.d.ts.map