import type { AuditHistoryItem } from '../types/audit.types';
export interface AuditEntryDetailProps {
    /** Entrada de auditoría a mostrar. Si es undefined, se muestra el placeholder. */
    entry?: AuditHistoryItem;
    /** Clases CSS adicionales para el contenedor. */
    className?: string;
}
//# sourceMappingURL=AuditEntryDetail.types.d.ts.map