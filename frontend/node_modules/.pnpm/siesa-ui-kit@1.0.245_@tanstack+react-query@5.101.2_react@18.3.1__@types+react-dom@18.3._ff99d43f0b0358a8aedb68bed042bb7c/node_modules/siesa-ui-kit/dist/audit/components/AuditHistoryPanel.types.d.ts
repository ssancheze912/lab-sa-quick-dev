import type { AuditHistoryProps } from '../types/audit.types';
export interface AuditHistoryPanelProps {
    /** Función que ejecuta la llamada al API de historial. */
    fetchFn: AuditHistoryProps['fetchFn'];
    /** ID de la entidad a consultar. */
    entityId: string;
    /** Tamaño de página. @default 20 */
    pageSize?: number;
    /** Clases CSS adicionales para el contenedor raíz. */
    className?: string;
}
//# sourceMappingURL=AuditHistoryPanel.types.d.ts.map