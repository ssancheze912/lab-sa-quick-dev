// Global setup for vitest - runs once before all tests
// No module mocking here - just environment setup
export function setup() {
  // nothing needed
}

export function teardown() {
  // nothing needed
}
