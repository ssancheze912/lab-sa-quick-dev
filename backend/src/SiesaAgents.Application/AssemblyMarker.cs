namespace SiesaAgents.Application;

/// <summary>
/// Empty marker used by reflection scans (FluentValidation, MediatR registrations).
/// CQRS handlers are added by subsequent stories.
/// </summary>
public static class AssemblyMarker
{
}
