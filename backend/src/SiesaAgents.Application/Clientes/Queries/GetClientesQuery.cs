namespace SiesaAgents.Application.Clientes.Queries;

/// <summary>
/// Query marker for retrieving the full clientes list. Story 2.1 keeps search
/// client-side, so no server-side parameters are required.
/// </summary>
public record GetClientesQuery();
