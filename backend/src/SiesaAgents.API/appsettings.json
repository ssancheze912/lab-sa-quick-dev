{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "_ConnectionStrings_Comment": "Placeholder only — production MUST override via env var ConnectionStrings__DefaultConnection, User Secrets, or Key Vault. NEVER commit real credentials.",
  "ConnectionStrings": {
    "DefaultConnection": ""
  }
}
