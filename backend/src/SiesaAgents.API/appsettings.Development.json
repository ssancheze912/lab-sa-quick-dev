{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedOrigins": [
    "http://localhost:5173"
  ],
  "_ConnectionStrings_Comment": "DEV ONLY — credentials below must be overridden via env vars in staging/production (User Secrets, Azure Key Vault, etc.)",
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Database=siesa_agents_db;Username=postgres;Password=postgres"
  }
}
