namespace SiesaAgents.Domain;

/// <summary>
/// Empty marker used by reflection scans (validators, EF Core configuration discovery, tests).
/// Domain entities are added by subsequent stories — keep this file as the assembly anchor.
/// </summary>
public static class AssemblyMarker
{
}
