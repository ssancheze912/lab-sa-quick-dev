namespace SiesaAgents.Infrastructure;

/// <summary>
/// Empty marker used by reflection scans (EF Core configuration discovery, repository registration).
/// AppDbContext + entity configurations are added by Story 1.3.
/// </summary>
public static class AssemblyMarker
{
}
