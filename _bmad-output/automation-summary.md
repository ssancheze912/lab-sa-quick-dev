# Automation Summary — Story 1.1: Project Initialization & Repository Structure

**Date:** 2026-06-08
**Story:** 1.1 (Epic 1: Project Foundation & Application Shell)
**Mode:** BMad-Integrated (expanding existing ATDD coverage)
**Coverage Target:** critical-paths + edge cases
**Story status:** review

---

## Context

ATDD baseline existed (generated by `testarch-atdd` sub-agent and verified 16/16
green in the implementation record). This automate pass expanded coverage with
edge cases, negative paths, and contract validations the ATDD intentionally
left out (per the BMad-Integrated automate principle: do not duplicate ATDD,
add what was missing).

### ATDD baseline (NOT regenerated)

- `e2e/tests/foundation/project-initialization.spec.ts` — 7 tests (AC1, AC3, AC4)
- `e2e/tests/api/backend-initialization.api.spec.ts` — 9 tests (AC2, AC5)

### Pre-existing edge spec (already created during earlier run)

- `e2e/tests/foundation/project-initialization.edge.spec.ts` — 7 frontend
  browser-context edge tests covering reload resilience, dotfile exposure,
  CORS browser-context, concurrent navigation.

---

## New automation generated in this pass

### API edge tests (new)

**File:** `e2e/tests/api/backend-initialization.edge.api.spec.ts` (12 tests, ~230 lines)

| # | Priority | Test | AC |
|---|----------|------|------|
| 1 | P0 | GET /health returns 200 + application/json | AC2 |
| 2 | P0 | GET /health body matches `{ status: 'ok' }` contract | AC2 |
| 3 | P2 | POST /health is not allowed (404/405, not 200) | AC2 |
| 4 | P2 | GET /scalar/ (canonical) serves HTML 200 | AC2 |
| 5 | P2 | GET /scalar (no slash) reaches the page after redirect | AC2 |
| 6 | P1 | OPTIONS /health preflight from allowed origin succeeds (200/204) | AC3 |
| 7 | P1 | CORS policy is pinned to the configured origin (never `*`) | AC3 |
| 8 | P1 | OPTIONS preflight from disallowed origin does NOT echo Allow-Origin | AC3 |
| 9 | P1 | 404 Problem Details has `{ status, title }` JSON shape | AC5 |
| 10 | P1 | 404 Problem Details does NOT leak stack traces / internals (NFR6) | AC5 |
| 11 | P2 | /swagger/* variants are not served (Swashbuckle forbidden) — 3 parametric tests | AC2 |
| 12 | P2 | OpenAPI document does not contain WeatherForecast schema | AC2 |

### Healed edge tests

| File | Test | Healing applied | Iterations |
|------|------|------------------|------------|
| `e2e/tests/foundation/project-initialization.edge.spec.ts` | `[P2] should load the Vite-generated favicon without HTTP 404` | Replaced flaky `page.waitForResponse(...)` race with deterministic `request.get('/favicon.svg')` direct fetch (timing-debugging pattern) | 1 |

No test required `test.fixme()` — all 37 tests in the foundation+api suite now pass.

---

## Test execution

```bash
pnpm exec playwright test --project=chromium e2e/tests/foundation e2e/tests/api
```

**Result:** 37 passed (11.3s)

Breakdown by file:
- `foundation/project-initialization.spec.ts` — 7 passed (ATDD)
- `foundation/project-initialization.edge.spec.ts` — 7 passed (1 healed)
- `api/backend-initialization.api.spec.ts` — 9 passed (ATDD)
- `api/backend-initialization.edge.api.spec.ts` — 14 passed (12 logical tests; one is parametric × 3 = 14 runs) **NEW**

---

## Coverage analysis

| AC | ATDD coverage | Automate expansion |
|----|---------------|---------------------|
| AC1 (Vite on 5173) | happy path | reload, concurrent contexts, dotfile non-exposure |
| AC2 (Backend + Scalar + 4 layers) | happy path | /health contract (status, content-type, body), method restriction, Scalar redirect, Swagger absence (3 URL variants), OpenAPI schema purity |
| AC3 (CORS) | preflight + Allow-Origin presence | wildcard absence, disallowed origin handling, /health CORS, browser-context CORS |
| AC4 (TS strict) | console clean, no overlay | (no expansion needed — covered by Vite compile + frontend unit tests) |
| AC5 (Backend builds) | runtime proxy | Problem Details RFC 7807 shape + NFR6 (no sensitive leakage) |

**Frontend unit tests already cover** (not regenerated):
- `frontend/src/shared/lib/apiClient.test.ts` — Axios baseURL + JSON header
- `frontend/src/shared/lib/queryClient.test.ts` — singleton + staleTime config

**Backend unit tests already cover** (not regenerated):
- `backend/tests/SiesaAgents.UnitTests/ApiSmokeTests.cs` — /health, Scalar, CORS
- `backend/tests/SiesaAgents.UnitTests/ExceptionHandlingMiddlewareTests.cs` — exception → ProblemDetails, no leak, passthrough

---

## Quality checks

- ✅ All new tests follow Given/When/Then with comments
- ✅ All new tests tagged with `[P0]`/`[P1]`/`[P2]` in test name
- ✅ All new tests deterministic — no `waitForTimeout`, no conditional flow
- ✅ All new tests self-contained — no shared state between tests
- ✅ New API spec uses `request` fixture (no UI dependency for API tests)
- ✅ No page objects (per BMad rule)
- ✅ Test file under 300 lines (`backend-initialization.edge.api.spec.ts` ~230 lines)
- ✅ Sandbox constraint honored — `--project=chromium` only
- ✅ Whole foundation+api suite green: 37/37 in 11.3s
- ✅ Healed favicon test no longer flaky

## DoD

- [x] Edge cases added on top of ATDD (not duplicated)
- [x] Negative paths covered (disallowed origin, forbidden methods, forbidden Swagger paths, sensitive-info leakage)
- [x] Contract validations added (`/health` body schema, Problem Details shape)
- [x] All generated tests pass on first run after one healing iteration
- [x] No test required `test.fixme()`
- [x] Automation summary written to `_bmad-output/automation-summary.md`

## Next steps

1. Story 1.1 already in `review` status — automate phase complete
2. Continue with `testarch-trace` / `testarch-review` for the quality gate
3. Future stories should follow the same dual-spec convention (`*.spec.ts` for
   ATDD acceptance, `*.edge.spec.ts` for automate edge cases)
