# Story 1.2: Frontend Navigation Shell

Status: ready-for-dev

## Story

As a user,
I want a persistent navigation structure to access the Clientes and Contactos sections of the application,
so that I can move between sections without full page reloads from any device.

## Acceptance Criteria

1. **Given** the application is loaded on a desktop browser (viewport ≥ 1024px), **When** the user views the app, **Then** the `LayoutBase` shell from siesa-ui-kit is rendered with a `NavigationRail` (72px collapsed, icon-only) on the left side containing "Clientes" and "Contactos" entries, and a `Navbar` at the top with `productName="Siesa Agents"`. (AC-E1.1, FR28)

2. **Given** the application is loaded on a desktop browser, **When** the user clicks the "Clientes" entry in the NavigationRail, **Then** the router navigates to `/clientes` without a full page reload (client-side navigation), and the shell layout remains mounted. (FR28)

3. **Given** the application is loaded on a desktop browser, **When** the user clicks the "Contactos" entry in the NavigationRail, **Then** the router navigates to `/contactos` without a full page reload, and the shell layout remains mounted. (FR28)

4. **Given** the application is loaded on a mobile browser viewport (< 1024px), **When** the user views the app, **Then** the `NavigationBar` (bottom nav, 56px) from siesa-ui-kit is displayed instead of the NavigationRail, and all navigation items ("Clientes", "Contactos") are accessible and tappable (WCAG 2.1 AA). (AC-E1.1, FR29)

5. **Given** the user types `/clientes` directly in the browser URL bar, **When** the page loads, **Then** the Clientes view is rendered correctly without redirection to a home screen, and the NavigationRail/NavigationBar indicates "Clientes" as the active item. (AC-E1.3, FR30)

6. **Given** the user types `/contactos` directly in the browser URL bar, **When** the page loads, **Then** the Contactos view is rendered correctly without redirection to a home screen, and the NavigationRail/NavigationBar indicates "Contactos" as the active item. (AC-E1.3, FR30)

7. **Given** the user navigates to the root path `/`, **When** the page loads, **Then** the router redirects to `/clientes` automatically (no blank screen at root).

8. **Given** the user navigates to an unknown route (e.g., `/ruta-inexistente`), **When** the page loads, **Then** a 404 / not-found view is displayed gracefully, and the shell layout (NavigationRail/NavigationBar) remains visible and functional.

## Tasks / Subtasks

- [ ] Task 1 - Set up TanStack Router file-based routing structure (AC: #5, #6, #7, #8)
  - [ ] Create `frontend/src/routes/__root.tsx` — root layout wrapping `LayoutBase` from siesa-ui-kit with `Outlet` for child routes
  - [ ] Create `frontend/src/routes/index.tsx` — redirects to `/clientes` via TanStack Router `redirect`
  - [ ] Create `frontend/src/routes/_app.tsx` — pathless layout route (`_` prefix = no URL segment) that wraps authenticated content area
  - [ ] Create `frontend/src/routes/_app/clientes.tsx` — `/clientes` route rendering `ClientesPlaceholder` (empty view with heading "Clientes")
  - [ ] Create `frontend/src/routes/_app/contactos.tsx` — `/contactos` route rendering `ContactosPlaceholder` (empty view with heading "Contactos")
  - [ ] Create `frontend/src/routes/$404.tsx` — catch-all not-found route (TanStack Router `notFoundComponent` or dedicated file) rendering a 404 view
  - [ ] Verify `routeTree.gen.ts` is auto-generated by the TanStack Router Vite plugin on `pnpm build`

- [ ] Task 2 - Implement `LayoutBase` shell with NavigationRail (desktop) (AC: #1, #2, #3)
  - [ ] In `frontend/src/routes/__root.tsx`, render `LayoutBase` from `siesa-ui-kit` with `navigationItems` prop containing entries for Clientes and Contactos
  - [ ] Configure `Navbar` with `productName="Siesa Agents"` inside LayoutBase
  - [ ] Configure `NavigationRail` (collapsed 72px) with Heroicons: use `UserGroupIcon` for Clientes, `UserIcon` for Contactos
  - [ ] Use TanStack Router `<Link>` components (or the router-aware nav items API from siesa-ui-kit) for navigation entries so routing is SPA-style (no `href` full reloads)
  - [ ] Apply active state styling to the current route's nav item (use `useRouterState` or `useMatch` from TanStack Router to detect active route)
  - [ ] All navigation labels MUST be in Spanish: "Clientes", "Contactos"

- [ ] Task 3 - Implement mobile-responsive NavigationBar (AC: #4)
  - [ ] In `frontend/src/routes/__root.tsx` (or `_app.tsx`), conditionally render `NavigationBar` (bottom nav) from siesa-ui-kit when viewport is < 1024px (Tailwind `lg:` breakpoint)
  - [ ] Hide `NavigationRail` on mobile and show `NavigationBar` — use Tailwind responsive utility classes (`hidden lg:flex` / `flex lg:hidden`), NOT JavaScript media query listeners
  - [ ] Ensure `NavigationBar` contains the same "Clientes" and "Contactos" entries with equivalent routing behavior
  - [ ] Verify touch targets meet WCAG 2.1 AA minimum (44x44px)

- [ ] Task 4 - Wire routing providers in `main.tsx` (AC: #2, #3, #5, #6)
  - [ ] Ensure `frontend/src/main.tsx` wraps `RouterProvider` (TanStack Router) inside `QueryProvider` (TanStack Query)
  - [ ] Configure `RouterProvider` with the generated `routeTree` from `routeTree.gen.ts`
  - [ ] Verify `pnpm run dev` starts on port 5173 with zero TypeScript errors after routing changes

- [ ] Task 5 - Create placeholder views for Clientes and Contactos routes (AC: #5, #6)
  - [ ] `frontend/src/modules/crm/clientes/presentation/ClientesPlaceholder.tsx` — functional component rendering `<h1>Clientes</h1>` (to be replaced in Epic 2)
  - [ ] `frontend/src/modules/crm/contactos/presentation/ContactosPlaceholder.tsx` — functional component rendering `<h1>Contactos</h1>` (to be replaced in Epic 3)
  - [ ] All user-facing text in Spanish

- [ ] Task 6 - Create NotFound view (AC: #8)
  - [ ] `frontend/src/shared/components/NotFound.tsx` — displays a user-friendly 404 message in Spanish (e.g., "Página no encontrada") with a link back to `/clientes`
  - [ ] Register as the not-found component in TanStack Router configuration

- [ ] Task 7 - Write component tests (AC: all)
  - [ ] `frontend/src/routes/__tests__/navigation.test.tsx` — test that NavigationRail renders "Clientes" and "Contactos" entries with correct router links (Vitest + RTL)
  - [ ] Test that clicking "Clientes" nav item navigates to `/clientes` without full reload (assert `window.location.reload` not called)
  - [ ] Test that clicking "Contactos" nav item navigates to `/contactos` without full reload
  - [ ] Test that navigating to `/ruta-inexistente` renders the NotFound component
  - [ ] Test that root path `/` redirects to `/clientes`
  - [ ] Test desktop viewport (1280px): NavigationRail visible, NavigationBar hidden
  - [ ] Test mobile viewport (375px): NavigationBar visible, NavigationRail hidden (or `display:none`)

## Dev Notes

### Architecture Decisions Applied

- **Routing**: TanStack Router file-based routing (`src/routes/` directory). The `_app` prefix creates a pathless layout route — it wraps child routes without adding a URL segment.
- **Navigation components**: `LayoutBase` + `NavigationRail` + `NavigationBar` from `siesa-ui-kit` (P0 mandatory — check siesa-ui-kit catalog BEFORE any custom implementation).
- **Responsive breakpoint**: `lg: 1024px` (Tailwind v4). Use CSS-only approach (`hidden lg:block` / `block lg:hidden`) — do NOT use JS `matchMedia` or resize listeners.
- **Active route detection**: Use TanStack Router's `useMatch` or `useRouterState` hooks. Do NOT rely on manual URL string comparison.
- **No Zustand store needed**: URL is the single source of truth for navigation state in MVP. No client state store required for this story.

### File Structure

```
frontend/src/
  routes/
    __root.tsx              # Root layout: LayoutBase + NavigationRail + Navbar
    index.tsx               # Redirects → /clientes
    _app.tsx                # Pathless layout route (content area)
    _app/
      clientes.tsx          # /clientes route
      contactos.tsx         # /contactos route
    $404.tsx or notFound.tsx # Not-found route
  modules/
    crm/
      clientes/
        presentation/
          ClientesPlaceholder.tsx
      contactos/
        presentation/
          ContactosPlaceholder.tsx
  shared/
    components/
      NotFound.tsx
```

### siesa-ui-kit Components Required

| Component | Usage | Props |
|-----------|-------|-------|
| `LayoutBase` | Root shell wrapper | `navigationItems`, children |
| `Navbar` | Top header bar | `productName="Siesa Agents"` |
| `NavigationRail` | Desktop left nav (72px collapsed) | `items`, active state |
| `NavigationBar` | Mobile bottom nav (56px) | `items`, active state |

> Before implementing, verify exact prop API in siesa-ui-kit documentation. Use MCP shadcn tool only if a component is NOT available in siesa-ui-kit.

### Routing Configuration

```typescript
// TanStack Router route tree (file-based, auto-generated)
// Routes:
//   / → redirect to /clientes
//   /clientes → ClientesPlaceholder (inside _app layout)
//   /contactos → ContactosPlaceholder (inside _app layout)
//   * → NotFound component

// __root.tsx: creates the persistent shell (LayoutBase) with Outlet
// _app.tsx: pathless layout, Outlet renders child route content
```

### Responsive Layout Pattern

```tsx
// Tailwind v4 responsive classes — CSS only, no JS
// NavigationRail: visible on desktop, hidden on mobile
<NavigationRail className="hidden lg:flex" ... />

// NavigationBar: visible on mobile, hidden on desktop
<NavigationBar className="flex lg:hidden" ... />
```

### Language Rule

ALL user-facing text MUST be in Spanish:
- Navigation labels: "Clientes", "Contactos"
- 404 page: "Página no encontrada", "Volver a Clientes"
- ARIA labels: `aria-label="Navegación principal"`

### Test Cases Covered (from test-design-epic-1.md)

| Test Case | Description |
|-----------|-------------|
| TC-E1-P1-01 | SPA navigation — no full page reload between routes |
| TC-E1-P1-02 | Deep linking — direct URL access to /clientes |
| TC-E1-P1-03 | Deep linking — direct URL access to /contactos |
| TC-E1-P1-04 | 404 route — unknown URL shows not-found view |
| TC-E1-P2-01 | NavigationRail visible on desktop viewport (1280px) |
| TC-E1-P2-02 | NavigationBar visible on mobile viewport (375px) |
| TC-E1-P2-03 | Index route redirects to /clientes |

### References

- Architecture decisions: [Source: _bmad-output/planning-artifacts/architecture.md]
- UX Design: [Source: _bmad-output/planning-artifacts/ux-design-specification.md — Direction F, Navigation Patterns section]
- Test design: [Source: _bmad-output/implementation-artifacts/test-design-epic-1.md]
- Company standards: [Source: .claude/agent-memory/sa-quick-dev/company-standards.md]
- Epic source: [Source: _bmad-output/planning-artifacts/epics/epic-01-foundation.md#Story 1.2]

## Dev Agent Record

### Agent Model Used

_to be filled by dev agent_

### Debug Log References

_to be filled by dev agent_

### Completion Notes List

_to be filled by dev agent_

### File List

_to be filled by dev agent_

## Review Follow-ups (AI)

_to be filled by code review agent_
