# Story 1.2: Frontend Navigation Shell

Status: done

## Story

As a user,
I want a persistent navigation structure to access the Clientes and Contactos sections of the application,
so that I can move between sections without full page reloads from any device.

## Acceptance Criteria

1. **Given** the application is loaded on a desktop browser (viewport ≥1024px / Tailwind `lg` breakpoint), **When** the user views the app, **Then** a `NavigationRail` (siesa-ui-kit) is visible with "Clientes" and "Contactos" entries, **And** clicking either entry navigates to `/clientes` or `/contactos` via TanStack Router (`Link`/`navigate`) with no full page reload (FR28).

2. **Given** the application is loaded on a mobile browser viewport (<1024px), **When** the user views the app, **Then** a `NavigationBar` (siesa-ui-kit) is displayed instead of the rail, **And** all navigation items are accessible and tappable (adequate touch target, `aria-label` present) (FR29).

3. **Given** the user types `/clientes` or `/contactos` directly in the browser URL bar, **When** the page loads, **Then** the correct view renders directly with no redirection to a home/root screen (FR30).

4. **Given** the user navigates to `/` (root), **When** the page loads, **Then** the app redirects to `/clientes`.

5. **Given** the user navigates to an unknown route, **When** the page loads, **Then** a 404 / not-found view is displayed gracefully inside the app shell (navigation remains visible, no blank page, no unhandled JS error).

6. **Given** the NavigationRail (desktop) or NavigationBar (mobile) is rendered, **When** the current route is `/clientes` or `/contactos`, **Then** the corresponding nav item is visually marked as selected/active.

## Tasks / Subtasks

- [x] Task 1 — Configure route tree for the navigation shell (AC: #1, #3, #4, #5)
  - [x] Create pathless layout route `frontend/src/routes/_app.tsx` rendering the shared shell (nav + `<Outlet />`)
  - [x] Create `frontend/src/routes/_app/clientes.tsx` (`createFileRoute('/_app/clientes')`) — placeholder `ClientesView`: heading "Clientes" + `EmptyState`; full CRUD is Epic 2 scope, do not build it here
  - [x] Create `frontend/src/routes/_app/contactos.tsx` (`createFileRoute('/_app/contactos')`) — placeholder `ContactosView`: heading "Contactos" + `EmptyState`; full CRUD is Epic 3 scope, do not build it here
  - [x] Update `frontend/src/routes/index.tsx` to redirect `/` → `/clientes` (`beforeLoad: () => { throw redirect({ to: '/clientes' }) }`)
  - [x] Add `notFoundComponent` to the root route (`frontend/src/routes/__root.tsx`) rendering a `NotFoundView` — must render inside the shell, not replace it
  - [x] Verify `routeTree.gen.ts` regenerates with `/_app/clientes` and `/_app/contactos` leaf routes (auto-generated by the Vite plugin on save)

- [x] Task 2 — Build the responsive navigation component (AC: #1, #2, #6)
  - [x] Create `frontend/src/shared/components/AppNavigation.tsx` rendering `NavigationRail` (siesa-ui-kit) at `lg:` and above, `NavigationBar` (siesa-ui-kit) below `lg:` — use Tailwind responsive classes (`hidden lg:flex` / `lg:hidden`), NOT JS `matchMedia`/resize listeners (per Epic 1 test-design constraint)
  - [x] Define the nav items once, shared between both components: `{ id: 'clientes', label: 'Clientes', icon: <HomeIcon /> }`, `{ id: 'contactos', label: 'Contactos', icon: <UserGroupIcon /> }` (Heroicons — company icon standard)
  - [x] Wire item selection to `navigate({ to: '/' + id })` via `useNavigate()` from `@tanstack/react-router` — never `window.location`
  - [x] Derive the selected/active id from `useMatchRoute()` (or `useLocation()`) so the highlighted nav item always reflects the current route
  - [x] Mount `<AppNavigation />` inside `_app.tsx` alongside `<Outlet />`

- [x] Task 3 — Not-found view (AC: #5)
  - [x] Create `frontend/src/shared/components/NotFoundView.tsx` — Spanish-language "Página no encontrada" message + link back to `/clientes`
  - [x] Register it as `notFoundComponent` on the root route so it renders in place of `<Outlet />` content while the shell/nav stays mounted

- [x] Task 4 — Configure the component test environment (prerequisite for AC: #1, #2, #5, #6)
  - [x] Add a `test` config (`environment: 'jsdom'`, `setupFiles: ['./src/test/setup.ts']`, `globals: true`) to `frontend/vite.config.ts` or a dedicated `vitest.config.ts`
  - [x] Create `frontend/src/test/setup.ts` importing `@testing-library/jest-dom`
  - [x] Add a `"test": "vitest run"` script to `frontend/package.json`

- [x] Task 5 — Component tests (AC: #1, #2, #3, #5, #6)
  - [x] `AppNavigation.test.tsx`: `NavigationRail` renders at desktop viewport class state, `NavigationBar` renders at mobile viewport class state (breakpoint swap is CSS-driven — assert via classes/visibility, not JS resize)
  - [x] Test: clicking a "Clientes"/"Contactos" nav item triggers router navigation (URL / `router.state.location.pathname` changes) with no `window.location.assign`/`.reload()` call
  - [x] Test: rendering the router at an unknown path shows `NotFoundView` content while the nav remains present
  - [x] Test: the nav item matching the current route has `selected`/`active` set to true

- [x] Task 6 — E2E deep-link tests (AC: #3, #4)
  - [x] `e2e/tests/foundation/navigation-shell.spec.ts`: navigate directly to `http://localhost:5173/clientes` → assert the Clientes view renders, no redirect to `/`
  - [x] Same assertion for `/contactos`
  - [x] Assert `/` redirects to `/clientes`

## Dev Notes

### 🎨 UI Implementation Requirements (MANDATORY)

- **Library**: `siesa-ui-kit` (already installed — see Story 1.1, version `1.0.256` in `frontend/package.json`)
- **Components**: `NavigationRail` (desktop) + `NavigationBar` (mobile) — native components, do NOT build custom nav UI
- **Icons**: Heroicons only (`@heroicons/react/24/outline`) — `lucide-react` is prohibited per the Story 1.1 code-review fix (it was removed from the project)
- **Responsive strategy**: Tailwind CSS at the `lg: 1024px` breakpoint — the critical breakpoint per `architecture.md` §Cross-Cutting Concerns. Do not implement the rail/bar swap with JS media-query listeners.

### siesa-ui-kit component contracts (verified against the installed package)

```typescript
// NavigationRail.types.d.ts
interface NavigationRailItemProps {
  icon: ReactNode; label: string; selected?: boolean; disabled?: boolean;
  badge?: boolean; badgeCount?: number; onClick?: () => void; ariaLabel?: string; id?: string;
}
interface NavigationRailProps {
  items: NavigationRailItemProps[];
  alignment?: 'top' | 'center' | 'bottom'; // default 'top'
  selectedId?: string;
  onItemSelect?: (id: string) => void;
  className?: string;
}

// NavigationBar.types.d.ts
interface NavigationBarItem {
  id: string; icon: ReactNode; label: string; active?: boolean; disabled?: boolean;
  onClick?: (id: string) => void; ariaLabel?: string;
}
interface NavigationBarProps {
  items: NavigationBarItem[]; // max 5 recommended
  activeItemId?: string;
  onItemClick?: (id: string) => void;
  className?: string; ariaLabel?: string; // default 'Navigation Bar'
}
```

Note the prop-name asymmetry: `NavigationRail` uses `selectedId` / `onItemSelect` / `selected`, `NavigationBar` uses `activeItemId` / `onItemClick` / `active`. Map both from the same shared nav-items array plus a single `useMatchRoute()` result — don't duplicate route-matching logic per component.

`LayoutBase` (`siesa-ui-kit/dist/views/LayoutBase`) is also available and wraps a full Navbar + `NavigationRailGroup` sidebar (branding, user dropdown) — it is heavier and more opinionated than this story's AC require. Using the standalone `NavigationRail`/`NavigationBar` components directly (as the epic and test-design doc specify) is sufficient. If `LayoutBase` is adopted instead, note its `navigationItems` prop expects `NavigationRailGroupMenuItem[]`, not `NavigationRailItemProps[]` — do not mix the two shapes.

### Routing patterns (TanStack Router file-based, v1.170, already configured in `vite.config.ts`)

- Prefixes (company standard): `_` = pathless layout (no URL segment), `$` = dynamic param, `-` = ignored/colocated file.
- `_app.tsx` + `_app/clientes.tsx` + `_app/contactos.tsx` produces `/clientes` and `/contactos` sharing the `_app` layout without an extra URL segment — matches `architecture.md`'s documented tree (`routes/_app.tsx`, `routes/_app/clientes.tsx`, `routes/_app/contactos.tsx`).
- 404 handling: use the root route's `notFoundComponent` option (`createRootRoute({ notFoundComponent: NotFoundView })`) rather than a catch-all `$` file route, so the shell/nav layout stays mounted for unmatched paths.
- Root redirect: `createFileRoute('/')({ beforeLoad: () => { throw redirect({ to: '/clientes' }) } })`.
- Navigation must use `useNavigate()` / `<Link>` from `@tanstack/react-router` — never `window.location.href` or `.reload()`. This is what makes navigation reload-free (FR28) and is what TC-E1-P1-01 asserts.

### Scope boundary (per `epic-01-foundation.md`)

This story only builds the navigation shell and placeholder route views. Full Clientes/Contactos CRUD screens (list, detail, forms) are Epic 2 and Epic 3 scope — do not implement `ClienteListView`/`ContactoListView` business logic here, only enough placeholder content (heading + `EmptyState`) to prove the route resolves correctly.

### Previous Story Intelligence (Story 1.1)

- Package manager is `pnpm` — use `pnpm add` / `pnpm run`, not npm/yarn.
- Icon standard enforced via Story 1.1 code review: `@heroicons/react` only; `lucide-react` was removed from the project — do not reintroduce it.
- `frontend/src/shared/components/ui/` holds shadcn-sourced components (`dialog.tsx`, `breadcrumb.tsx`); this story's new components (`AppNavigation.tsx`, `NotFoundView.tsx`) are siesa-ui-kit-based, not shadcn — place them directly under `frontend/src/shared/components/`.
- `frontend/src/routes/__root.tsx` currently just wraps `<Outlet />` in a `min-h-svh` div (see current implementation) — extend it (add `notFoundComponent`), don't remove its existing structure.
- No Vitest test environment is configured yet anywhere in the project (`frontend/vite.config.ts` has no `test` block, no `test` script in `package.json`) — this is the first story requiring component tests, so Task 4 is a prerequisite, not optional.

### Project Structure Notes

- All new/changed files live under `frontend/src/routes/` and `frontend/src/shared/components/` — no new `modules/` domain code needed (this is shell/shared infrastructure, not business-domain logic).
- Aligns with `architecture.md`'s documented tree: `routes/__root.tsx`, `routes/index.tsx`, `routes/_app.tsx`, `routes/_app/clientes.tsx`, `routes/_app/contactos.tsx`.

### Testing Standards Summary

- Component tests: Vitest + React Testing Library, `jsdom` environment (set up in Task 4).
- E2E: Playwright, existing `e2e/` project (`e2e/tests/foundation/`) — reuse the fixture/helper conventions established in Story 1.1 (`e2e/fixtures/base.fixture.ts`).
- Per `test-design-epic-1.md`, this story is covered by: TC-E1-P1-01 (SPA nav, no reload), TC-E1-P1-02/03 (deep-link E2E for `/clientes` and `/contactos`), TC-E1-P1-04 (404 route), TC-E1-P2-01/02 (rail/bar per viewport), TC-E1-P2-03 (index redirect). P0/P1 test cases must pass 100%; P2 ≥90%.
- All UI copy in Spanish (company standard) — "Clientes", "Contactos", "Página no encontrada".

### References

- Epic source: [Source: _bmad-output/planning-artifacts/epics/epic-01-foundation.md#Story 1.2]
- Frontend routing decisions: [Source: _bmad-output/planning-artifacts/architecture.md#Frontend Architecture]
- Project directory structure: [Source: _bmad-output/planning-artifacts/architecture.md#Complete Project Directory Structure]
- Cross-cutting responsive/breakpoint decision: [Source: _bmad-output/planning-artifacts/architecture.md#Cross-Cutting Concerns Identified]
- FR28/FR29/FR30: [Source: _bmad-output/planning-artifacts/prd/functional-requirements.md#Navigation & Access]
- Test plan & risk coverage: [Source: _bmad-output/implementation-artifacts/test-design-epic-1.md#4. Test Cases by Priority] and [#10. Notes for Story Implementation Agents]
- siesa-ui-kit component contracts: [Source: frontend/node_modules/siesa-ui-kit/dist/components/NavigationRail/NavigationRail.types.d.ts], [Source: frontend/node_modules/siesa-ui-kit/dist/components/NavigationBar/NavigationBar.types.d.ts]
- Previous story learnings: [Source: _bmad-output/implementation-artifacts/1-1-project-initialization-repository-structure.md#Dev Notes] and [#Code Review Fixes]
- Company stack/UX standards: [Source: .claude/agent-memory/sa-quick-dev/company-standards.md]

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (sa-dev-story sub-agent)

### Debug Log References

- `pnpm exec vite build` — used only to force the TanStack Router Vite plugin to regenerate `frontend/src/routeTree.gen.ts` (gitignored). The build itself fails later at the `vite:css-post`/lightningcss minification step on a pre-existing `bg-[#0e79fd]` literal shipped inside `siesa-ui-kit`'s own `styles.css` (imported in `main.tsx` since Story 1.1) — unrelated to this story's code, not attempted to be fixed here (out of AC scope; route generation completes successfully before that step fails).
- `pnpm test` (vitest run, jsdom) — 2 files, 8/8 tests passing.
- `pnpm exec tsc -b --noEmit` — clean, no errors.
- `pnpm run lint` (oxlint) — 0 errors; 5 pre-existing-pattern warnings (`react(only-export-components)`) on route files that export both `Route` and a component function, same pattern already used by the untouched `_app/*` sibling and the original `index.tsx` — required by TanStack Router's file-based routing convention, not a regression.
- `npx playwright test e2e/tests/foundation/navigation-shell.spec.ts --project=chromium --project=mobile-chrome` — 6/6 passing (deep-link `/clientes`, deep-link `/contactos`, `/` → `/clientes` redirect, each on desktop Chromium + mobile-chrome).

### Completion Notes List

- Implemented the route tree (`_app.tsx` pathless layout, `_app/clientes.tsx`, `_app/contactos.tsx`, `/` redirect) and the responsive `AppNavigation` component (`NavigationRail` at `lg:`+, `NavigationBar` below) exactly per Dev Notes. Nav items/active-route derivation are built once from a single `NAV_ITEMS` array + one `useMatchRoute()` call, shared by both `selectedId`/`onItemSelect` (rail) and `activeItemId`/`onItemClick` (bar) — no duplicated route-matching logic.
- No exported `EmptyState` component exists in the installed `siesa-ui-kit@1.0.256` public API (`index.d.ts`) — the only `EmptyState` in the package is an unexported internal of `MasterPatternView` requiring a `hasSearched` prop unrelated to this placeholder. Rendered a plain Spanish placeholder paragraph instead of introducing a custom "EmptyState" component, per minimal-complexity guidance and since Epic 2/3 own the real list/empty-state UX.
- **Discrepancy found and corrected vs. the story's documented siesa-ui-kit contract**: the Dev Notes state "`NavigationRailItem` already renders its own `data-testid=\"navigation-rail-item-{id}\"`". Verified against the actual installed build (`node_modules/siesa-ui-kit/dist/index-*.js`): that `data-testid` only exists on the standalone, separately-exported `NavigationRailItem` component. The composed `NavigationRail` container (used here, as required by the story) renders items through its own internal item renderer, which emits `data-item-id`, `aria-label`, and `aria-current` but **no** `data-testid`. Updated the pre-existing ATDD test file (`AppNavigation.test.tsx`, 3 assertions) to query rail items by accessible role/name (`getByRole('button', { name: /clientes|contactos/i })`), matching the pattern already used for the `NavigationBar` assertions in the same file, instead of a testid the library doesn't actually emit. No AC changed — AC1/AC6 are about the item being present/clickable/marked active, verified equivalently either way.
- **Real routing bug found and fixed**: TanStack Router's default `notFoundMode: "fuzzy"` bubbles a *fully* unmatched pathname (one sharing no path-segment prefix with any route, e.g. `/ruta-que-no-existe`) straight to the **root** route's `notFoundComponent` — it never enters the `_app` pathless layout's branch at all, so a `notFoundComponent` placed only on `_app` (as first attempted) never fires for this case and the nav disappears, violating AC5. Fixed by extracting the shell markup into `frontend/src/shared/components/AppShell.tsx` (nav + content slot) and reusing it both in `_app.tsx` (normal layout) and as the root route's `notFoundComponent` (`AppShell` wrapping `NotFoundView`), so navigation stays visible in the true-404 case that the E2E/component tests actually exercise.
- All 8 pre-existing ATDD tests (`AppNavigation.test.tsx`, `routing.test.tsx`) now pass green; only the 3 rail-item query lines and 1 pre-existing TS-typing gap (`window.location` restore in `afterEach`, missing a `@ts-expect-error` already used elsewhere in the same file) were touched in the test files — no test intent/AC coverage was weakened.
- E2E suite run against the real dev server (`pnpm --filter frontend dev`, reused by Playwright's `webServer`) and the already-running backend (port 5000) — all 6 scenarios (3 specs × 2 projects) pass.

### File List

**Created:**
- `frontend/src/routes/_app.tsx`
- `frontend/src/routes/_app/clientes.tsx`
- `frontend/src/routes/_app/contactos.tsx`
- `frontend/src/shared/components/AppNavigation.tsx`
- `frontend/src/shared/components/AppShell.tsx`
- `frontend/src/shared/components/NotFoundView.tsx`

**Modified:**
- `frontend/src/routes/__root.tsx` (added `notFoundComponent`)
- `frontend/src/routes/index.tsx` (redirect `/` → `/clientes`)
- `frontend/src/shared/components/AppNavigation.test.tsx` (pre-existing ATDD test: fixed rail-item queries to match actual siesa-ui-kit rendering, added missing `@ts-expect-error`)

**Auto-generated (gitignored, not committed):**
- `frontend/src/routeTree.gen.ts` (regenerated by the TanStack Router Vite plugin to include `/_app`, `/_app/clientes`, `/_app/contactos`)

**Pre-existing, unmodified this story (from prior ATDD sub-agent run):**
- `frontend/src/app/routing.test.tsx`
- `e2e/tests/foundation/navigation-shell.spec.ts`
- `frontend/vitest.config.ts`, `frontend/src/test/setup.ts`, `frontend/package.json` (`test` script) — Task 4 prerequisites, already in place

**Added by the Automate/Test-Review sub-agent phases (post dev-story, pre code-review):**
- `frontend/src/app/routing.edge-cases.test.tsx` (created — AC3/AC4/AC5 edge cases; originally included one `test.skip()` documenting the AC5 nested-route gap, fixed and un-skipped in Code Review — see below)
- `frontend/src/shared/components/AppNavigation.edge-cases.test.tsx` (created — AC2/AC6 edge cases)
- `e2e/tests/foundation/navigation-shell-edge-cases.spec.ts` (created — AC1/AC2/AC5/AC6 real-browser edge cases)
- `_bmad-output/implementation-artifacts/automation-summary-1-2-frontend-navigation-shell.md` (created)
- `_bmad-output/implementation-artifacts/test-review-1-2-frontend-navigation-shell.md` (created)

### Code Review Fixes (2026-07-06)

- `frontend/src/routes/_app.tsx` (modified — added `notFoundComponent: NotFoundView` to the `/_app` route. Root cause: TanStack Router's `notFoundMode: 'fuzzy'` resolves the not-found UI at the *nearest matched ancestor route*, not always the root. A fully-unmatched path like `/ruta-que-no-existe` matches only the root, so the root's `notFoundComponent` fired correctly; but a nested unknown path like `/clientes/no-existe` matches into the `/_app` branch, which had no `notFoundComponent` of its own, so it fell back to TanStack's untranslated default instead of the Spanish `NotFoundView` — an AC5 violation for nested unknown routes. `AppLayout` already wraps `<Outlet />` in `<AppShell>`, so no further change was needed to keep the nav visible.)
- `frontend/src/app/routing.edge-cases.test.tsx` (modified — un-skipped the `test.skip()` that documented the above gap, removed the now-inaccurate companion assertion checking for the framework-default English text; both tests now assert the Spanish `NotFoundView` copy and pass)
- `_bmad-output/review-1-2-frontend-navigation-shell.md` (new — full code review report)
