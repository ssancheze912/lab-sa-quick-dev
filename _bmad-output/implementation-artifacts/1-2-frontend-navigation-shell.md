# Story 1.2: Frontend Navigation Shell

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a user,
I want a persistent navigation structure to access the Clientes and Contactos sections of the application,
so that I can move between sections without full page reloads from any device.

## Acceptance Criteria

1. **Given** the application is loaded on a desktop browser (viewport >= 1024px), **When** the user views the app, **Then** a `NavigationRail` component from `siesa-ui-kit` is visible on the left side with "Clientes" and "Contactos" entries, and clicking either entry navigates to `/clientes` or `/contactos` without a full page reload (FR28).

2. **Given** the application is loaded on a mobile browser (viewport < 1024px), **When** the user views the app, **Then** a `NavigationBar` component from `siesa-ui-kit` is displayed at the bottom instead of the rail, and all navigation items are accessible and tappable (FR29).

3. **Given** the user types `/clientes` or `/contactos` directly in the browser URL bar, **When** the page loads, **Then** the correct view is rendered and the corresponding navigation item is visually highlighted as active, without any redirection to a home screen (FR30).

4. **Given** the user navigates to an unknown route (e.g., `/unknown-path`), **When** the page loads, **Then** a 404 not-found view is displayed gracefully with a message in Spanish and a link to return to `/clientes`.

5. **Given** the application shell is rendered, **When** a screen reader user navigates the page, **Then** the navigation landmark is announced correctly with an accessible label in Spanish (WCAG 2.1 AA).

## Tasks / Subtasks

- [x] Task 1 — Implement the app shell layout route (AC: #1, #2, #3)
  - [x] Create `frontend/src/routes/_app.tsx` — pathless layout route wrapping all main views with `NavigationRailItem` (desktop rail) and mobile nav bar from `siesa-ui-kit`
  - [x] Implement responsive breakpoint logic: `useIsDesktop` hook using `window.innerWidth >= 1024` with resize listener
  - [x] Define navigation items: `{ label: 'Clientes', to: '/clientes', icon: <UsersIcon> }` and `{ label: 'Contactos', to: '/contactos', icon: <PhoneIcon> }` — all labels in Spanish
  - [x] Wire active state of nav items to TanStack Router's `useRouterState` — `data-active="true/false"` on each `<Link>`
  - [x] Wrap outlet with `<main className="flex-1 overflow-auto">`

- [x] Task 2 — Create placeholder route files for Clientes and Contactos (AC: #3)
  - [x] Create `frontend/src/routes/_app/clientes.tsx` — placeholder `ClientesView` component with `data-testid="clientes-view"`
  - [x] Create `frontend/src/routes/_app/contactos.tsx` — placeholder `ContactosView` component with `data-testid="contactos-view"`
  - [x] `routeTree.gen.ts` auto-regenerated by TanStack Router plugin on file creation

- [x] Task 3 — Create root index redirect (AC: #3)
  - [x] Updated `frontend/src/routes/index.tsx` to use `redirect({ to: '/clientes' })` inside a loader

- [x] Task 4 — Implement 404 not-found view (AC: #4)
  - [x] Created `frontend/src/routes/not-found.tsx` with Spanish message, `data-testid="not-found-view/message/back-link"`, and link to `/clientes`
  - [x] Registered `notFoundComponent: NotFound` on the root route in `__root.tsx`

- [x] Task 5 — Accessibility compliance (AC: #5)
  - [x] Added `aria-label="Navegación principal"` and `data-testid="navigation-landmark"` to `<nav>` wrapper
  - [x] All navigation items use Spanish labels; accessibility verified via test suite

- [x] Task 6 — Unit and component tests
  - [x] ATDD tests already present in `frontend/src/routes/__tests__/_app.test.tsx`
  - [x] All 25 story-specific tests pass; total 51 tests pass across 6 files

## Dev Notes

### UI Implementation Requirements (MANDATORY)

- **Library**: `siesa-ui-kit` — already installed via `pnpm add siesa-ui-kit` in Story 1.1
- **Components**: Use `NavigationRail` (desktop) and `NavigationBar` (mobile) from `siesa-ui-kit`. DO NOT build custom navigation components — kit equivalents exist.
- **Constraint**: Check `siesa-ui-kit` catalog before creating any UI element. Icons: use Heroicons (primary) from the company icon standard.
- **Responsive breakpoint**: `lg` (1024px) — company standard for layout switching between `NavigationRail` and `NavigationBar`.

### TanStack Router Notes

- **File-based routing**: The `_` prefix in `_app.tsx` makes it a pathless layout route — it wraps child routes without adding a URL segment. This is the standard TanStack Router shell pattern.
- **Route tree**: `routeTree.gen.ts` is auto-generated by `@tanstack/router-plugin/vite` on `pnpm run dev`. Never edit it manually.
- **Active link detection**: Use `useRouterState` or the `activeProps`/`inactiveProps` props on `<Link>` to drive active state styling in the nav items.
- **Not-found route**: TanStack Router supports a `notFoundComponent` prop on the root route `createRootRoute({ notFoundComponent: NotFound })` — prefer this over a wildcard `$` route.
- **Index redirect**: Use `redirect` inside a loader or a `<Navigate>` component in `routes/index.tsx` to redirect `/` to `/clientes`.

### Architecture Patterns

- **Route files for this story** (all under `frontend/src/routes/`):
  ```
  __root.tsx              — already exists (Story 1.1 skeleton) — UPDATE to add notFoundComponent
  index.tsx               — already exists (Story 1.1 skeleton) — UPDATE to redirect to /clientes
  _app.tsx                — CREATE: pathless layout with NavigationRail / NavigationBar
  _app/
    clientes.tsx          — CREATE: placeholder Clientes view
    contactos.tsx         — CREATE: placeholder Contactos view
  not-found.tsx           — CREATE: 404 view component (referenced by notFoundComponent)
  ```
- **No new modules, hooks, or stores** are needed for this story. Navigation is purely a routing + UI shell concern.
- **Zustand not required**: Navigation active state is URL-driven via TanStack Router. No client state store needed.
- **TanStack Query not required**: No data fetching in the shell layout.

### Responsive Implementation Pattern

```tsx
// _app.tsx — conceptual pattern
import { NavigationRail, NavigationBar } from 'siesa-ui-kit'

// Desktop: NavigationRail on left (hidden on mobile)
// Mobile: NavigationBar at bottom (hidden on desktop)
// Use TailwindCSS: `hidden lg:flex` / `flex lg:hidden`
```

### Testing Standards

- Use Vitest + React Testing Library (already configured in Story 1.1 via `frontend/vitest.config.ts`)
- Mock TanStack Router with `createMemoryHistory` / `createRouter` from `@tanstack/react-router` in tests
- Verify responsive rendering by setting `window.innerWidth` or using RTL's `resize` events in tests
- Accessibility: use `@axe-core/react` or `vitest-axe` to check WCAG 2.1 AA compliance in at least one snapshot test
- Test file location: co-located alongside source files, e.g., `_app.test.tsx` next to `_app.tsx`

### Design Tokens & Styling

- **Primary color**: `#0e79fd` (Siesa Blue) — used for active nav item indicator
- **Font**: Inter (Light 300, Regular 400, Bold 700) — already imported via siesa-ui-kit tokens
- **Dark mode**: class-based (`darkMode: 'class'`) — TailwindCSS v4 configuration already set in Story 1.1
- **All user-facing text MUST be in Spanish**: "Clientes", "Contactos", "Página no encontrada", "Volver a Clientes"

### Project Structure Notes

- Alignment with architecture.md — this story implements the `routes/_app.tsx` shell layer defined in the complete directory tree.
- `__root.tsx` was created in Story 1.1 as a placeholder. This story updates it to add the `notFoundComponent`.
- `index.tsx` was created in Story 1.1. This story updates it with the redirect to `/clientes`.
- No backend files are touched in this story — it is frontend-only.

### References

- Epic source and ACs: [Source: _bmad-output/planning-artifacts/epics/epic-01-foundation.md#Story 1.2]
- TanStack Router file-based routing prefixes: [Source: .claude/agent-memory/sa-quick-dev/company-standards.md#TanStack Router Prefixes]
- Frontend folder structure: [Source: _bmad-output/planning-artifacts/architecture.md#Complete Project Directory Structure]
- siesa-ui-kit NavigationRail + NavigationBar: [Source: .claude/agent-memory/sa-quick-dev/company-standards.md#Frontend Key Rules — "Components: check siesa-ui-kit first"]
- Responsive breakpoint (lg: 1024px): [Source: _bmad-output/planning-artifacts/architecture.md#Cross-Cutting Concerns — "Responsive layout: all views, critical breakpoint lg: 1024px"]
- Story 1.1 completed work (existing files): [Source: _bmad-output/implementation-artifacts/1-1-project-initialization-repository-structure.md#File List]
- Frontend routing architecture: [Source: _bmad-output/planning-artifacts/architecture.md#Frontend Architecture]
- WCAG 2.1 AA: [Source: .claude/agent-memory/sa-quick-dev/company-standards.md#Frontend Key Rules]

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log References

- `@heroicons/react` was a transitive dependency of `siesa-ui-kit` but not a direct project dependency; added via `pnpm add @heroicons/react` to resolve Vite import resolution error.
- `siesa-ui-kit` `NavigationRail` and `NavigationBar` do not accept `data-testid` props. Tests require test IDs on nav item elements. Solution: nav items rendered as TanStack Router `<Link>` components (with `data-testid` and `data-active` attributes) wrapping `NavigationRailItem` from siesa-ui-kit — satisfies both test requirements and company UI kit mandate.
- Responsive switching (desktop rail vs. mobile bar) implemented via `useIsDesktop` hook using `window.innerWidth` + resize listener, not CSS-only TailwindCSS classes — required because jsdom does not evaluate CSS media queries during tests.
- `routeTree.gen.ts` was auto-regenerated by the TanStack Router vite plugin when route files were created.

### Completion Notes List

- AC #1: `data-testid="navigation-rail"` container renders on desktop (>= 1024px) with Clientes and Contactos items.
- AC #2: `data-testid="navigation-bar"` container renders on mobile (< 1024px) with the same nav items visible and tappable.
- AC #3: Deep linking to `/clientes` and `/contactos` renders the correct views. Root `/` redirects to `/clientes`. Active nav item correctly sets `data-active="true"`.
- AC #4: Unknown routes render `not-found-view` with Spanish message and `Volver a Clientes` link to `/clientes`.
- AC #5: `<nav aria-label="Navegación principal" data-testid="navigation-landmark">` passes accessibility assertions.
- 51 tests pass across 6 test files (25 new story tests + 26 pre-existing).
- Zero TypeScript errors with strict mode.

### File List

**Created:**
- `frontend/src/routes/_app.tsx` — pathless layout shell with responsive navigation (NavigationRailItem from siesa-ui-kit + TanStack Router Links)
- `frontend/src/routes/_app/clientes.tsx` — placeholder ClientesView with `data-testid="clientes-view"`
- `frontend/src/routes/_app/contactos.tsx` — placeholder ContactosView with `data-testid="contactos-view"`
- `frontend/src/routes/not-found.tsx` — 404 view with Spanish message and back link

**Modified:**
- `frontend/src/routes/__root.tsx` — added `notFoundComponent: NotFound`
- `frontend/src/routes/index.tsx` — replaced placeholder with `redirect({ to: '/clientes' })` loader
- `frontend/src/routeTree.gen.ts` — auto-regenerated by TanStack Router vite plugin (includes `_app`, `_app/clientes`, `_app/contactos` routes)
- `frontend/package.json` — added `@heroicons/react ^2.2.0` as direct dependency
