# Automation Summary - Story 2.5: Delete Client

**Date:** 2026-06-28
**Story:** 2-5-delete-client
**Epic:** 2 - Client Management
**Mode:** BMad-Integrated
**Coverage Target:** critical-paths (expanded to edge cases)

---

## Existing ATDD Tests (Baseline — Already Passing)

These tests were generated by the `testarch-atdd` workflow and were passing at story completion. They are NOT modified.

### Frontend Component Tests (Vitest + RTL + MSW)
File: `frontend/src/modules/crm/clientes/__tests__/DeleteCliente.test.tsx` — 15 tests

- TC-E2-2-5-CMP-P0-1 [P0] Confirm delete → DELETE called once, invalidateQueries triggered, onClienteDeleted called
- TC-E2-2-5-CMP-P1-1 [P1] Cancel delete → dialog closes, no DELETE called
- TC-E2-2-5-CMP-P1-2 [P1] Delete (204) → toast "Cliente eliminado correctamente"
- TC-E2-2-5-CMP-P2-1 [P2] Delete (200+hadContacts:true) → toast with "Sus contactos asociados..."
- Plus 11 additional ATDD tests covering AC #1–#4 behavior

### Backend API Tests (xUnit + WebApplicationFactory)
File: `backend/tests/SiesaAgents.UnitTests/Clientes/DeleteClienteApiTests.cs` — 4 tests

- TC-E2-2-5-API-P0-1 [P0] DELETE valid ID → 204 No Content
- TC-E2-2-5-API-P1-1 [P1] DELETE unknown UUID → 404 Problem Details
- Tech debt stub for Epic 3 (contacts cascade)
- DELETE removes client from list (FR27)

### E2E Tests (Playwright)
File: `e2e/tests/clientes/clientes-delete.spec.ts` — 5 tests
File: `e2e/tests/api/clientes-delete.api.spec.ts` — 8 tests

---

## New Tests Generated (Automation Expansion)

### Component Tests — Edge Cases (Vitest + RTL + MSW)
File: `frontend/src/modules/crm/clientes/__tests__/DeleteCliente.edge.test.tsx`

**13 new tests**

| Priority | Test Scenario |
|----------|---------------|
| [P1] | Dialog state resets — re-opening after cancel shows fresh state (no stale state) |
| [P1] | Open dialog second time → successful deletion works correctly |
| [P1] | Double-click guard — "Confirmar" disabled mid-flight prevents second DELETE call |
| [P1] | "Eliminando..." text appears on button while DELETE is pending |
| [P1] | 404 DELETE response → error toast shown, onClienteDeleted NOT called |
| [P1] | Dialog closes even when DELETE returns 404 (onError calls setIsDeleteDialogOpen(false)) |
| [P2] | Network error → error toast shown, onClienteDeleted NOT called |
| [P2] | Cache eviction: ['clientes', id] query entry removed after successful deletion |
| [P3] | btn-eliminar NOT rendered when GET returns 404 (not-found state) |
| [P3] | btn-eliminar NOT rendered when GET returns 500 (error state) |
| [P1] | Exact toast text "Cliente eliminado correctamente" — no partial match (R-010) |
| [P1] | Exact toast text "...contactos asociados..." — strict text enforcement (R-010) |
| [P2] | 200 + {hadContacts: false} shows generic toast (not contacts variant) |

### Backend API Edge Case Tests (xUnit + WebApplicationFactory)
File: `backend/tests/SiesaAgents.UnitTests/Clientes/DeleteClienteApiEdgeTests.cs`

**13 new tests**

| Priority | Test Scenario |
|----------|---------------|
| [P1] | DELETE with non-UUID ID (not-a-uuid) → 400/404, NOT 500 (route constraint) |
| [P1] | DELETE with integer ID (12345) → 400/404, NOT 500 (route constraint) |
| [P1] | DELETE same ID twice → second call returns 404 Problem Details (idempotency) |
| [P1] | DELETE 204 response body is exactly empty (not even whitespace) |
| [P2] | DELETE 404 Title = "Cliente no encontrado" (exact Spanish text) |
| [P2] | DELETE 404 Detail contains "El cliente solicitado no fue encontrado." (exact) |
| [P2] | DELETE 404 Status field = 404 (not 0 or null — RFC 7807) |
| [P2] | DELETE 404 no stack traces in response (NFR6 enforcement) |
| [P2] | DELETE deletes only target client — other clients remain untouched |
| [P2] | After DELETE: client not accessible by ID AND not in list (dual persistence check) |
| [P3] | DELETE nil UUID (all zeros) → 404 Problem Details, NOT 500 |
| [P3] | DELETE max UUID (all f's) → 404 Problem Details, NOT 500 |
| [P2] | 200 response body shape {hadContacts: camelCase boolean} (Epic 3 tech debt documented) |

### E2E Edge Case Tests (Playwright)
File: `e2e/tests/clientes/clientes-delete-edge-cases.spec.ts`

**7 new tests**

| Priority | Test Scenario |
|----------|---------------|
| [P1] | DELETE 500 → error toast shown, URL stays at /clientes/:id (no navigation) |
| [P1] | btn-eliminar has aria-label attribute (WCAG 2.1 AA) |
| [P1] | "Confirmar" button disabled during in-flight DELETE (full-stack isPending guard) |
| [P2] | Dialog description "Esta acción no se puede deshacer." is shown (AC #1) |
| [P2] | URL returns to /clientes (no clienteId) after successful deletion (navigation contract) |
| [P2] | Left panel list no longer contains deleted client (FR27 — no reload) |
| [P3] | Navigating to deleted client URL shows not-found state (bookmark edge case) |

---

## Coverage Analysis

**Total New Tests:** 33 (13 Component + 13 Backend + 7 E2E)

**Priority Breakdown:**
- P0: 0 (all P0 scenarios fully covered by ATDD baseline)
- P1: 17 tests (10 Component + 2 Backend + 5 E2E)
- P2: 12 tests (2 Component + 7 Backend + 2 E2E edge cases overlap with ATDD)
- P3: 4 tests (2 Component + 2 Backend)

**Test Levels:**
- E2E: 7 new tests (edge path flows)
- API (Backend): 13 new tests (boundary conditions, error paths, security)
- Component (Frontend): 13 new tests (UI state edge cases, error handling)
- Unit: 0 (no pure logic functions without dependencies in this story)

**Tests marked as fixme:** 0

---

## Coverage Gaps Addressed

| Gap | ATDD Coverage | Expansion Coverage |
|-----|--------------|-------------------|
| Dialog re-open state reset | Not covered | DeleteCliente.edge.test.tsx [P1] |
| Double-click prevention | Not covered | DeleteCliente.edge.test.tsx [P1] |
| 404 DELETE response as error | Not covered | DeleteCliente.edge.test.tsx [P1] |
| Network error handling | Not covered | DeleteCliente.edge.test.tsx [P2] |
| TanStack Query cache eviction | Not covered | DeleteCliente.edge.test.tsx [P2] |
| btn-eliminar hidden in error states | Partial (undefined) | DeleteCliente.edge.test.tsx [P3] |
| Exact toast text (R-010) | Partial (regex match) | DeleteCliente.edge.test.tsx [P1] |
| hadContacts=false in 200 body | Not covered | DeleteCliente.edge.test.tsx [P2] |
| Malformed UUID route rejection | Not covered | DeleteClienteApiEdgeTests.cs [P1] |
| DELETE idempotency (2nd call→404) | Not covered | DeleteClienteApiEdgeTests.cs [P1] |
| 204 empty body enforcement | Not covered | DeleteClienteApiEdgeTests.cs [P1] |
| NFR6 no stack traces in 404 | Basic check | DeleteClienteApiEdgeTests.cs [P2] |
| Client isolation (delete A, B intact) | Not covered | DeleteClienteApiEdgeTests.cs [P2] |
| Nil/Max UUID boundary | Not covered | DeleteClienteApiEdgeTests.cs [P3] |
| 500 → stay at URL (E2E) | Not covered | clientes-delete-edge-cases.spec.ts [P1] |
| aria-label accessibility | Not covered | clientes-delete-edge-cases.spec.ts [P1] |
| Dialog description text (E2E) | Not covered | clientes-delete-edge-cases.spec.ts [P2] |
| Bookmark deleted URL → not-found | Not covered | clientes-delete-edge-cases.spec.ts [P3] |

---

## Remaining Deferred Items (Not Expanded — Epic 3 dependency)

These scenarios require the Contactos table (Epic 3) and are documented as tech debt:

- TC-E2-2-5-API-P0-2: Cascade ON DELETE SET NULL — contacts get clienteId=null
- TC-E2-2-5-API-P2-1: DELETE with contacts → 200 + {hadContacts: true} (live data)
- E2E P0 deferred: delete + toast "...contactos asociados..." (requires running Contactos)

All three are covered by ATDD tests that self-skip when Epic 3 is not available.

---

## Definition of Done — Automation Expansion

- [x] All tests follow Given-When-Then format
- [x] All tests have priority tags [P0]–[P3]
- [x] Frontend tests use MSW network-first pattern (handlers before render)
- [x] Backend tests use WebApplicationFactory (real HTTP stack)
- [x] E2E tests use page.route() network-first pattern (intercept before navigation)
- [x] No hard waits or flaky patterns (no waitForTimeout)
- [x] Self-cleaning tests (afterEach cleanup)
- [x] Deterministic test data (buildCliente factory)
- [x] No duplicate coverage with ATDD baseline tests
- [x] No tests marked as fixme
- [x] TypeScript strict mode — no `any` types in frontend tests
- [x] Test files under 400 lines

---

## Test Execution

```bash
# Run frontend component tests (ATDD + expansion)
cd frontend && pnpm test --run src/modules/crm/clientes/__tests__/DeleteCliente.edge.test.tsx

# Run all Delete-related frontend tests
cd frontend && pnpm test --run src/modules/crm/clientes/__tests__/DeleteCliente

# Run backend API edge case tests
cd backend && dotnet test --filter "DeleteClienteApiEdgeTests" --logger "console;verbosity=normal"

# Run all Delete backend tests
cd backend && dotnet test --filter "DeleteCliente" --logger "console;verbosity=normal"

# Run E2E delete edge cases (requires running frontend + backend)
npx playwright test e2e/tests/clientes/clientes-delete-edge-cases.spec.ts

# Run all delete-related E2E
npx playwright test e2e/tests/clientes/clientes-delete
```

---

## Next Steps

1. Run the new tests locally to confirm zero failures (implementation complete)
2. Integrate with CI pipeline (tests auto-run on PR)
3. When Epic 3 is implemented: re-enable deferred cascade tests
4. Run `bmad tea *trace` to update traceability matrix with new test IDs
